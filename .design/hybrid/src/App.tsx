import { Fragment, useEffect, useRef, useState } from 'react';
import { copy } from './copy';
import { Gradient, Grain } from './Gradient';
import { Name } from './Name';
import { Rack } from './Rack';
import { Stars } from './Stars';
import { DeliveryDemo } from './Folders';
import { useReveal, useFocusScrub, usePriceReveal } from './reveal';
import { useSmoothScroll } from './smooth-scroll';
import { TiltFrame } from './Tilt';
import { Fan } from './Fan';
import { PricePlate } from './PricePlate';
/* `FilmCaption` больше не импортируется (Ф40): EXIF-подпись снята с
   плёночной карточки вместе с одиночным кадром — см. комментарий у веера
   плёночной ниже. Сам `FilmCaption.tsx` НЕ удалён и остаётся рабочим
   компонентом; здесь висит эта строка, а не мёртвый импорт, потому что
   неиспользуемый импорт следующий читатель просто вычистит, не узнав, что
   компонент жив и ждёт возврата. */
import { SectionStars } from './SectionStars';
/* `NavHint` больше не импортируется (Ф64) — стрелка-подсказка снята, причина
   записана у места, где она стояла, в `Page()`. Компонент жив и ждёт возврата,
   поэтому здесь строка комментария, а не мёртвый импорт: тот же приём, что у
   `FilmCaption` ниже. */
/* Ф63 — блок-приглашение «Придумать съёмку» на главной (`Storyboard()` ниже).
   Импортируется `q0.ts`, а НЕ `questions.ts`: второй тянет за собой `pick.ts`,
   то есть весь алгоритм подбора кадров второй страницы, и он уехал бы в
   сборку главной целиком. Причина записана подробно в самом `q0.ts`. */
import { Q0_TILES, Q0_TITLE, POVOD_PARAM, POVOD_SLUG } from './storyboard/q0';

/**
 * ГЛАВНАЯ. Гибрид направлений B и C по `DIRECTION-LOCK.md`.
 *
 * Весь экранный текст — из `copy.ts`: `COPY.md` ред. 6 плюс правки Ф28, где
 * владелица дала формулировки дословно. Ни одна строка не переписана и не
 * сокращена под макет. Где текст не встал — записано в README как находка.
 *
 * Композиция сознательно не карточная. Одинаковых сеток на странице нет ни
 * одной: рэк — предмет в руке, «Как получается кино» — диптих со сбитой осью,
 * услуги — четыре разновеликих разворота «кадр + текст», разделённые
 * волосяными линейками, «Как это устроено» — лестница, FAQ — вопрос слева
 * крупно, ответ справа узкой мерой.
 *
 * РЕДАКЦИЯ 2 — правки Ф28 после первого живого показа:
 *   1. имя статично, морфинга нет (`Name.tsx` вместо `NameMorph.tsx`);
 *   2. тема одна, зелёная; переключатель и `theme.tsx` удалены;
 *   3. звёзды светятся (`Stars.tsx`);
 *   4. навигация переехала из подвала в шапку, шесть подписей Ф28;
 *   5. из подвала убраны навигация по фотографиям и упоминание юрстатуса;
 *   6. CTA сведён к одной фразе со ссылкой на слове «сюда»;
 *   7. подпись-триггер — на самой папке, пояснение — словами владелицы;
 *   8. фотографии услуг заменены, добавлен слот «плёнка», показ уменьшен;
 *   9. секция «Шесть кадров» не рендерится (зоны и компонент сохранены).
 *
 * РЕДАКЦИЯ 3 (Ф29, FACTS.md — «След + Кино без звука», НЕ лок, семь пунктов
 * владелицы поверх редакции 2, «фиксируем, делай!»). Подробности каждого —
 * в комментариях затронутых файлов, здесь только список и где искать:
 *   1. настроение диптиха «Как получается кино» непрерывно со скроллом —
 *      `Gradient.tsx` (`--mood`), `.mesh-mood-a/b` в styles.css;
 *   2. курсор-метка «тяни» над рэком вместо системного grab — `Rack.tsx`;
 *   3. 3D-тилт карточек услуг по курсору — `Tilt.tsx`, здесь ниже;
 *   4. боковой индикатор разделов звёздами — `SectionStars.tsx`;
 *   5. EXIF-подпись на плёночном кадре — `FilmCaption.tsx`, сужена до слова
 *      «плёнка» без города и техданных (OQ-21, находка записана там же);
 *   6. цена за кликом внутри плашки — `PricePlate.tsx`;
 *   7. асимметричный разлёт папок отдачи — `Folders.tsx`, styles.css.
 * Явно НЕ реализовано (прямая просьба владелицы «без звуков»): звук затвора
 * камеры при перевороте рэка. В коде нет ни одного звукового API — проверено
 * `grep`, дублируется измерением в `selfcheck.mjs`.
 *
 * РЕДАКЦИЯ 5 (Ф36 + Ф37 п.1–4, FACTS.md — «Редакция 5 главной страницы»,
 * НЕ лок). Девять пунктов Ф36 плюс четыре правки Ф37 поверх редакции 3.
 * Подробности и решения билдера — README § 10, здесь только список:
 *   1. навигация: «Работы» снята, «Обо мне» — вторым пунктом, «Подготовка
 *      к съёмке» → «С чего начать» (и якорь ссылки переехал, см. `NAV` ниже);
 *   2. первый экран: второе предложение лида переписано под её пример;
 *   3. рэк вырос с 8 до 12 кадров — `Rack.tsx`, `scripts/export-photos.mjs`;
 *   4. первая ссылка первого экрана мельче (`t-lead` вместо `t-h3`);
 *   5. диптих «Как получается кино»: новый текст, метки-заголовки сняты;
 *   6. крупные секционные заголовки (`.t-h1`) уменьшены — `styles.css`;
 *   7. новая вводная строка блока услуг;
 *   8. карточки услуг: новые фото/имена/цены «город — цена/час», плотнее
 *      вёрстка (фото рядом с текстом, не над ним), плашка цены открыта
 *      сразу без клика («дверца» снизу вверх), четвёртая карточка
 *      «Плёночная», ссылка «Весь прайс и условия» снята;
 *   9. «Как это устроено» → «С чего начать и как это всё устроено», новый
 *      текст шагов 1–4 (шаги 3–4 — правка Ф37 п.1–2 поверх Ф36);
 *  10. (Ф37 п.3) подпись-пояснение под папками отдачи переехала НАД них;
 *  11. (Ф37 п.4) секция «Помогу подготовиться к съёмке» не рендерится.
 *
 * Ф39 (не лок), пять правок владелицы:
 *   1. «обо мне» поднят сразу после первого экрана — `<AboutTeaser />`
 *      переставлена в `Page()` из конца страницы за `<Hero />`, порядок секций
 *      совпал с порядком пунктов навигации; якорь `#o-mne` не менялся;
 *   2. H1 «Создаю ваше кино» «чуть чуть меньше» — `--step-4` в `styles.css`,
 *      минус 10% по всей вилке;
 *   3. две ссылки первого экрана сведены в одну «Написать» на Telegram —
 *      `copy.hero.ctaSecondary` выключена, см. `Hero()`;
 *   4. в «Кто снимает» слева встала фотография автора, ссылка «Обо мне» из-под
 *      текста снята — см. `AboutTeaser()` и `export-photos.mjs`;
 *   5. новый текст «Кто снимает» — ОЖИДАЕТСЯ, владелица переписывает.
 */

export function App() {
  return (
    <>
      <Gradient />
      <Page />
      <Grain />
    </>
  );
}

function Page() {
  useReveal();
  useFocusScrub();
  // Плашка цены на тач-устройствах — двусторонний reveal по скроллу
  // (см. `usePriceReveal()` в `reveal.ts`); на десктопе не действует.
  usePriceReveal();
  // Ф39 п.14 — плавный ход колеса, см. `smooth-scroll.ts`
  /* ═══ ПЕРЕХВАТ КОЛЕСА СНЯТ (Ф44) ══════════════════════════════════════
     «Все анимации приятны и плавны, но когда листаешь сайт, он всё равно с
     трудом двигается, как будто немного тормозя».

     Эта пара — «анимации плавные, а прокрутка тяжёлая» — и есть диагноз.
     Анимации живут на композиторе: браузер крутит их отдельно и не ждёт
     основной поток. Родная прокрутка тоже идёт на композиторе — поэтому
     страница листается гладко даже когда основной поток занят.

     `useSmoothScroll` (Ф39, её же просьба «сделай более плавный скролл»)
     отменял ровно это. Он гасил событие колеса и двигал страницу сам,
     `window.scrollTo` на каждом кадре, — то есть ПЕРЕНОСИЛ ПРОКРУТКУ С
     КОМПОЗИТОРА НА ОСНОВНОЙ ПОТОК. А там очередь: зерно, размытие шапки,
     полотно, звёзды. Пока всё это считается, прокрутка ждёт — и получается
     именно «двигается с трудом», при том что анимации рядом идут ровно.
     Просьба была верной, средство — нет: я сделала прокрутку хуже там, где
     хотела сделать лучше.

     Плавность переходов по якорям НЕ ПОТЕРЯНА: её даёт `scroll-behavior:
     smooth` на `html` (`styles.css`), она стояла там и до Ф39, работает на
     стороне браузера и колеса не касается. Нажатия на пункты навигации
     по-прежнему едут плавно.

     Заодно снимается вся защита, которую пришлось к перехвату городить
     (Ф40, «сделай ссылки снова кликабельными»): нет перехвата — нет и
     спора двух прокруток за одну ось. */

  /* Ф45 ВОЗВРАЩАЕТ ХУК — но уже на расчищенный основной поток, и только для
     мыши. Полный разбор, что изменено против Ф39 и почему это теперь не
     повторит Ф44, — в шапке `smooth-scroll.ts`.
     ЕСЛИ ТЯЖЕСТЬ ПРОКРУТКИ КОГДА-НИБУДЬ ВЕРНЁТСЯ — снимать надо ЭТУ строку
     первой, до всяких раскопок: она единственная переносит прокрутку с
     композитора на основной поток. */
  useSmoothScroll();

  return (
    <>
      <Header />
      {/* Боковой индикатор разделов (Ф29 п.4) — fixed, читает `main > section`
          сам, поэтому положение в дереве не важно; стоит здесь, рядом с
          другими сквозными слоями шапки/подвала. */}
      <SectionStars />
      {/* ═══ СТРЕЛКА-ПОДСКАЗКА СНЯТА (Ф64) ══════════════════════════════
          Владелица: «тогда стрелку указатель можно вообще убрать, раз
          раздел, на который мы хотим, чтобы обращали внимание, находится в
          лендинге».

          Это закрывает фичу Ф33 п.3 не отменой, а достижением цели: стрелка
          существовала ровно затем, чтобы указать на спрятанный раздел. С Ф63
          раздел стоит на самой странице и показывает себя первым вопросом —
          указывать больше не на что, а указатель, переживший свою причину,
          читается как реклама поверх контента.

          `NavHint.tsx` НЕ УДАЛЁН и остаётся рабочим компонентом (тот же
          принцип, что `Grid()` и `Faq()`): возврат — одна строка здесь.
          `id="nav-link-storyboard"` у пункта шапки тоже оставлен — это
          адрес, по которому подсказка себя вешала, и без него возврат
          потребовал бы правки в двух местах вместо одного. */}
      <main id="main" className="relative z-10">
        <Hero />
        {/* «Обо мне» — Ф39 п.1: «подними раздел "обо мне" наверх, чтобы он шёл
            сразу после раздела "главное"». Секция переехала сюда из конца
            страницы (стояла между `How()` и `Cta()`), порядок остальных не
            тронут. Теперь порядок секций совпадает с порядком навигации, где
            «Обо мне» стоит вторым пунктом с Ф36 п.1. Ни сама секция, ни её
            якорь `#o-mne` не изменились — переехал только узел в дереве. */}
        <AboutTeaser />
        <Cinema />
        {/* <Grid /> — ВЫКЛЮЧЕНО Ф28: «6 фотографий, которые ты выбрал, я бы
            убрала пока. если что, добавим позже». Компонент и зоны `copy.grid`
            оставлены на месте по прямому требованию Ф28 не удалять зоны:
            возврат секции — одна строка, а не восстановление кода. */}
        <Offer />
        {/* «Придумать съёмку» на главной — Ф63, см. `Storyboard()` ниже.
            Стоит сразу после услуг по прямому указанию владелицы: «поставить
            раздел "придумать съемку" после раздела "что снимаю"». Порядок
            секций страницы теперь совпадает с порядком пунктов навигации
            целиком, впервые за всё время. */}
        <Storyboard />
        <How />
        {/* <Faq /> — ВЫКЛЮЧЕНО Ф37 п.4: «раздел "помогу подготовиться к
            съемке" вообще убираем». Тот же принцип, что `<Grid />` (Ф28):
            компонент и зоны `copy.faq` сохранены на месте, не удалены —
            возврат секции остаётся одной строкой. Якорь `#podgotovka`,
            который вёл сюда из навигации, теперь указывает на `How()`
            (переименована «С чего начать и как это всё устроено», Ф36
            п.9) — см. комментарий у `NAV` ниже и README § 10. */}
        {/* <AboutTeaser /> — ПЕРЕЕХАЛА наверх, сразу после `<Hero />` (Ф39
            п.1), см. комментарий там. */}
        <Cta />
      </main>
      <Footer />
    </>
  );
}

const WRAP = 'relative z-10 mx-auto w-full max-w-[var(--measure)]';
const SECT = 'relative px-[var(--gutter)]';

/* ═════════════════════════ ШАПКА ═════════════════════════
   Позиция, размер и звезда-компаньон — из направления C (лок, «Имя — облик из C»):
   фиксированная строка сверху, звезда слева от имени, кегль как в C.
   Единственное добавление к C — подложка, которая появляется, когда шапка
   уезжает на контент: в C под ней было сплошное тёмное поле, здесь под ней
   живой градиент и фотографии, и без подложки имя ложится на кадр.

   НАВИГАЦИЯ (Ф28) — здесь, а не в подвале: «навигацию сделай в верхушке сайта,
   а не внизу». Состав и порядок дословно из Ф28, ПРАВЛЕНЫ Ф36 п.1: «в
   навигации убери ссылку "работы". раздел "обо мне" подними и сделай сразу
   после "главная". ссылку "подготовка к съемке" переименуй в "с чего
   начать"» — «Работы» снята целиком, «Обо мне» переставлена на второе
   место, «Подготовка к съёмке» → «С чего начать» (подпись).

   АДРЕСА. Собранная страница на сегодня одна, главная. Страниц «Обо мне»,
   «Что снимаю», «С чего начать» ещё нет — это задача Ф4, и выдумывать их
   сейчас было бы подменой архитектуры. Поэтому пункты ведут в соответствующие
   по смыслу зоны ЭТОЙ страницы, а не на несуществующие URL: ссылка на 404
   хуже ссылки на секцию. Соответствие — в таблице `NAV` ниже, каждый
   переходный адрес помечен `stub: true`, чтобы его нельзя было принять
   за готовый раздел при следующей правке. Это заявленный долг (README).

   НАХОДКА РЕДАКЦИИ 5 (не лок, координатор, README § 10). Ф36 п.1 переименовал
   ссылку «Подготовка к съёмке» → «С чего начать», но её `href` указывал на
   `#podgotovka` — якорь секции `Faq()`. Ф37 п.4 следом убрал ЭТУ ЖЕ секцию
   («раздел "помогу подготовиться к съемке" вообще убираем») целиком. Если
   оставить `href` как был, «С чего начать» вела бы в никуда — ссылка на
   несуществующий якорь хуже, чем на 404, потому что молчит, не 404. Новое
   название пункта («С чего начать») дословно совпадает с началом нового
   заголовка секции `How()` («С чего начать и как это всё устроено», Ф36
   п.9) — семантически это она и есть. Решение билдера: якорь `#podgotovka`
   переехал на `How()` (был без `id`), ссылка ведёт туда же адресом, что и
   раньше, — ни новых URL, ни новых зон COPY, чистая перепайка маршрута. */
/* Ссылки — с ведущим «/», не голым «#…». На ЭТОЙ странице ведущий слэш ничего
   не меняет (путь `/` совпадает с текущим, переход внутристраничный, без
   перезагрузки — фрагментная навигация по спецификации). Но те же подписи
   теперь используются и с новой страницы `storyboard.html` (фича «Придумать
   съёмку», добавлена ниже) — оттуда голый `#uslugi` увёл бы в никуда (такого
   узла на той странице нет), а `/#uslugi` корректно возвращает на главную и
   прокручивает к разделу. Один список ссылок работает с обеих страниц. */
const NAV: { label: string; href: string; stub: boolean; id?: string }[] = [
  // «Главная» — начало страницы. Раньше туда вело имя; Ф28 сделала имя
  // некликабельным, и переход наверх достался этому пункту.
  { label: 'Главная', href: '/#main', stub: false },
  // «Обо мне» — Ф36 п.1 подняла её на второе место (была после «Работы»,
  // которая теперь снята целиком).
  { label: 'Обо мне', href: '/#o-mne', stub: true },
  /* «Выбрать съёмку» — Ф63, переименование дословно владелицей: «раздел "что
     снимаю" переименовать в "выбрать съемку"». Причина её же: «пользователь
     не сразу понял про что этот раздел» — прежняя подпись обещала СЮЖЕТЫ
     («что я снимаю» — поля, портреты), а внутри форматы съёмок и цены.
     Адресат не менялся: та же секция `home:offer` — «Что можно у меня
     снять». Сам ЗАГОЛОВОК секции не тронут: просили переименовать пункт,
     и её текст правится только её словами. */
  { label: 'Выбрать съёмку', href: '/#uslugi', stub: false },
  // «Придумать съёмку» — НОВЫЙ пункт (`BRIEF-STORYBOARD.md`), формулировка
  // подтверждена владелицей дословно. Полноценная страница, не модалка на
  // главной (раздел 5 брифа) — `storyboard.html`, вторая точка входа сборки
  // (см. `vite.config.mts`), не хэш-якорь этой страницы.
  // `id` — только у этой ссылки: адресуется стрелкой-подсказкой `NavHint.tsx`
  // (Ф33 п.3), которой нужно измерить реальное положение пункта на экране.
  { label: 'Придумать съёмку', href: '/storyboard.html', stub: false, id: 'nav-link-storyboard' },
  // «С чего начать» — Ф36 п.1, переименована из «Подготовка к съёмке» (Ф27).
  // `href` не изменился (`#podgotovka`), но адресат переехал: раньше это был
  // якорь `Faq()`, теперь — `How()` (см. находку выше, README § 10).
  { label: 'С чего начать', href: '/#podgotovka', stub: false },
  { label: 'Контакты', href: '/#kontakt', stub: false },
];

/* Экспортирован (не только используется в `Page` этого файла): страница
   `storyboard.html` (фича «Придумать съёмку») переиспользует этот же `Header`
   и `Footer` дословно — правило «общее раньше частного», один источник
   правды для шапки/навигации/подвала на весь сайт, не два похожих файла,
   которые разойдутся при следующей правке. */
export function Header() {
  const [set, setSet] = useState(false);
  const [open, setOpen] = useState(false);

  useEffect(() => {
    const onScroll = () => setSet(window.scrollY > 40);
    onScroll();
    window.addEventListener('scroll', onScroll, { passive: true });
    return () => window.removeEventListener('scroll', onScroll);
  }, []);

  /* Список на узком экране закрывается по Esc. Раскрытый список, который
     нельзя закрыть клавиатурой, ловит фокус: пользователь табом уходит в
     скрытый от глаз контент. */
  useEffect(() => {
    if (!open) return;
    const onKey = (e: KeyboardEvent) => e.key === 'Escape' && setOpen(false);
    window.addEventListener('keydown', onKey);
    return () => window.removeEventListener('keydown', onKey);
  }, [open]);

  const links = NAV.map((n) => (
    <li key={n.label}>
      <a
        id={n.id}
        className="nav-link"
        href={n.href}
        data-stub={n.stub ? '' : undefined}
        onClick={() => setOpen(false)}
      >
        {n.label}
      </a>
    </li>
  ));

  return (
    <header
      className={`fixed left-0 right-0 top-0 z-50 px-[var(--gutter)] py-[clamp(0.9rem,1.6vw,1.4rem)] transition-[background-color] duration-300 ${
        set || open
          ? /* Ф44: РАЗМЫТИЕ ПОДЛОЖКИ СНЯТО, вместо него плотный фон.
               `backdrop-filter: blur()` — вторая по цене вещь на первом
               экране после зерна, и включалась она ровно там, где владелица
               жалуется: шапка получает этот класс, как только страница
               сдвинулась на 40 px, то есть с первым же движением колеса.
               Дальше на каждом кадре браузер обязан взять полосу под шапкой
               и размыть её заново — полоса едет, значит результат каждый раз
               новый и закэшировать его нельзя.
               Задача подложки была одна: чтобы имя и навигация не ложились
               на фотографию. Непрозрачность её решает без единого пикселя
               размытия. 72% → 92%: сквозь неё по-прежнему угадывается
               полотно, но текст читается на любом кадре.
               Обратимо одной строкой, если захочется вернуть стекло. */
            'bg-[color-mix(in_srgb,var(--paper)_92%,transparent)]'
          : ''
      }`}
    >
      <div className="mx-auto flex max-w-[var(--measure)] items-center justify-between gap-[clamp(1rem,3vw,2.5rem)]">
        {/* Имя — не ссылка (Ф28: «пусть оно не будет кликабельным»). */}
        <Name />

        {/* Одна разметка навигации на все ширины, одна `<nav>` на документ:
            два `<nav>` с одним и тем же списком — это дубль лендмарка, а на
            кадре он выглядит ровно как один. Широкая раскладка — строка,
            узкая — раскрывающийся список. */}
        <nav aria-label="Разделы сайта" className="min-w-0">
          <button
            type="button"
            className="nav-toggle md:hidden"
            aria-expanded={open}
            aria-controls="nav-list"
            onClick={() => setOpen((v) => !v)}
          >
            {/* Слово, а не иконка: весь служебный слой сайта набран словами.
                «Разделы» — не экранный текст из COPY, а подпись органа
                управления; в `COPY.md` такой зоны нет, и это записано в
                хендоф как долг копирайтера. */}
            Разделы
          </button>
          <ul
            id="nav-list"
            className={`m-0 list-none flex-wrap items-baseline gap-x-[clamp(0.9rem,1.8vw,1.9rem)] gap-y-3 p-0 ${
              open
                ? /* Раскрытый список узкой раскладки. Оговорки `md:` возвращают
                     строку, если экран расширили с открытым списком: без них
                     на планшете висела бы полноширинная плашка поверх контента. */
                  'nav-sheet absolute left-0 right-0 top-full flex flex-col gap-y-[0.9rem] px-[var(--gutter)] py-[clamp(1.1rem,3vh,1.8rem)] md:static md:flex-row md:bg-transparent md:px-0 md:py-0 md:[backdrop-filter:none]'
                : 'hidden md:flex'
            }`}
          >
            {links}
          </ul>
        </nav>
      </div>
    </header>
  );
}

/* ═════════════════════════ ПЕРВЫЙ ЭКРАН ═════════════════════════
   Лестница заголовка — приём cur-makemepulse: три ступени с растущим отступом.
   H1 три слова (COPY ред. 6), поэтому кегль поднят до 136 px на 1920: на пяти
   словах прошлой редакции такой кегль не помещался, на трёх помещается.
   Справа — рэк: один предмет в пустоте, и он же механика внимания (П13). */
function Hero() {
  return (
    <section
      id="hero"
      className={`${SECT} flex min-h-[100svh] flex-col justify-center pb-[clamp(3rem,8vh,6rem)] pt-[clamp(6rem,14vh,9rem)]`}
    >
      <Stars count={18} seed={7} scale="mixed" />

      <div className={WRAP}>
        <div className="grid grid-cols-1 items-end gap-x-[var(--gap)] gap-y-[clamp(2.4rem,6vw,4rem)] lg:grid-cols-12">
          <div className="lg:col-span-7 lg:col-start-1">
            <p className="reveal t-mono text-[color:var(--ink-mute)]">{copy.hero.geo}</p>

            {/* Лестница из трёх блоков. Между блоками СТОИТ пробельный узел, и он
                здесь не косметика: три `<span className="block">` вплотную дают
                textContent «Создаювашекино» — именно это и было в первой версии.
                Визуально пробел между блочными элементами не виден, а копирование,
                поиск и скринридер получают «Создаю ваше кино», то есть H1 дословно
                (правило П8: текст заголовка остаётся текстом, даже когда он
                разложен композицией). */}
            <h1 className="reveal t-display mt-[clamp(1rem,2.4vw,2rem)]" style={{ ['--i' as string]: 1 }}>
              {copy.hero.h1.map((word, i) => (
                <Fragment key={word}>
                  {i > 0 ? ' ' : null}
                  <span className="block" style={{ paddingLeft: `${[0, 1.1, 2.4][i]}em` }}>
                    {word}
                  </span>
                </Fragment>
              ))}
            </h1>

            <p
              className="reveal t-lead mt-[clamp(1.6rem,3.4vw,2.6rem)] max-w-[36ch] text-[color:var(--ink-soft)]"
              style={{ ['--i' as string]: 2 }}
            >
              {copy.hero.lead}
            </p>

            <div
              className="reveal mt-[clamp(1.8rem,4vw,3rem)] flex flex-wrap items-baseline gap-x-[2.4rem] gap-y-4"
              style={{ ['--i' as string]: 3 }}
            >
              {/* Ф28 дала настоящий адрес личного Telegram (`@arisheniaa`, не
                  канал `@byarisheniaa`). Ссылка, которая говорит «Написать»,
                  обязана открывать Telegram, а не прокручивать страницу к
                  блоку контактов, как было до появления адреса.
                  РЕДАКЦИЯ 5 (Ф36 п.4): «текст про написать в телеграм сделай
                  чуть меньше» — было `t-h3` (step-2, до 1.95rem), стало
                  `t-lead` (step-1, до 1.4rem).

                  Ф39 п.2: обе ссылки первого экрана сведены в одну — «Написать»
                  одним словом, ведёт туда же. Вторая («Сначала посмотреть
                  съёмки», вела на рэк `#raboty`) не рендерится, зона
                  `copy.hero.ctaSecondary` сохранена дормантной. Кегль
                  `t-lead` НЕ поднят обратно: Ф36 п.4 уменьшила эту ссылку
                  отдельной просьбой, и то, что сосед исчез, — не повод
                  молча отменять прошлую правку. Обёртка `flex-wrap` с двумя
                  зазорами оставлена как есть: на одном ребёнке она ведёт
                  себя ровно как обычная строка, а возврат второй ссылки не
                  потребует переписывать раскладку. */}
              {/* `link-tap` (Ф42) — ссылка стоит отдельным блоком, а не внутри
                  предложения, поэтому ей можно дать зону касания под палец,
                  не задевая соседние строки. Правило — `styles.css`. */}
              <a
                className="link-major link-tap t-lead"
                href="https://t.me/arisheniaa"
                target="_blank"
                rel="noopener noreferrer"
              >
                {copy.hero.ctaPrimary}
              </a>
            </div>
          </div>

          {/* СТОПКА ИЗ 12 СНИМКОВ — Ф40, «серию из 12 снимков на главной
              сделай чуть выше и ближе к фразе».

              БЛИЖЕ: колонка старта 9 → 8. Текст занимает 1–7, то есть
              восьмая — первая свободная: ближе стопку не подвинуть, не
              заехав на заголовок. Между ними остаётся один гаттер, а не
              гаттер плюс пустая колонка, как было.

              ВЫШЕ: у ряда стоит `items-end` — стопка равнялась по нижнему
              краю текстового блока, ровно на одной линии со ссылкой
              «Написать». Полем снизу поднимаем её над этой линией, не
              трогая выравнивание всего ряда: `items-center` увёл бы стопку
              к середине текста, а это уже не «чуть». Величина в `vh`, а не
              в пикселях: подъём должен считаться от высоты экрана — на
              низком окне первый экран и так тесный, и фиксированные
              пиксели съели бы там воздух под стопкой.

              Ф40, второй заход: «подними ещё чуть выше» — 8vh → 15vh,
              потолок 5 → 9 rem. На окне 720 это ещё 50 px вверх: стопка
              встаёт верхним краем вровень с началом заголовка, а не ниже
              его. Дальше поднимать уже нельзя — упрётся в шапку. */}
          <div
            id="raboty"
            className="reveal scroll-mt-[7rem] lg:col-span-4 lg:col-start-8 lg:mb-[clamp(3rem,15vh,9rem)]"
            style={{ ['--i' as string]: 4 }}
          >
            <Rack />
          </div>
        </div>
      </div>
    </section>
  );
}

/* ═════════════════════════ КАК ПОЛУЧАЕТСЯ КИНО ═════════════════════════
   Ни одной фотографии: пара произносится типографикой на пустоте. Ось сбита
   намеренно — две равные колонки читались бы как таблица, а половины
   равновесны по весу, не по сетке. Звёзды здесь крупные и бордовые: единственное
   место, где они работают предметом, а не фоном.

   `data-mood-scope` (Ф29 п.1, FACTS.md — не лок): по этому узлу `Gradient.tsx`
   считает локальный прогресс прохода ИМЕННО этой секции и двигает им фон
   между «тихим» и «странным» непрерывно, синхронно со скроллом — см.
   комментарий в Gradient.tsx и `.mesh-mood-a/b` в styles.css.

   РЕДАКЦИЯ 5 (Ф36 п.5): метки-заголовки «ПРИДУМАНО ЗАРАНЕЕ» / «НАЙДЕНО НА
   МЕСТЕ» (были `copy.cinema.a.label`/`.b.label`, набраны `.t-mono`) сняты —
   текст остаётся, подписи над ним больше нет. */
function Cinema() {
  return (
    /* Ф40: «в разделе как получается кино тоже — текст должен помещаться в
       один экран». Раздел был 717 px при окне 720 — формально влезал, на
       деле упирался в оба края без единого пикселя воздуха, а на окне чуть
       ниже (ноутбук с открытой панелью закладок) уже не влезал.

       Основной расход был не в тексте, а в полях: 15vh сверху и снизу —
       216 px из 717, почти треть секции на пустоту. Это было сделано
       намеренно (диптиху нужен воздух больше, чем остальным), но «один
       экран» и «самые большие поля на странице» — взаимоисключающие
       требования, и выбрано первое. 15vh → 8vh, отступ до диптиха 8vh →
       5vh, сдвиг правой половины 8vh → 4.5vh. Сбитая ось сохранена: правая
       половина по-прежнему ниже левой, просто не на треть экрана.
       Меры строк не тронуты — текст набирается ровно так же. */
    <section data-mood-scope className={`${SECT} py-[clamp(3rem,8vh,7rem)]`}>
      <Stars count={9} seed={19} scale="bold" tones={[2, 0]} shapes={['hands5', 'starfish6', 'spark']} />

      <div className={WRAP}>
        <h2 className="reveal t-h1 max-w-[16ch]">{copy.cinema.title}</h2>

        <div className="mt-[clamp(2rem,5vh,4rem)] grid grid-cols-1 gap-x-[var(--gap)] gap-y-[clamp(2rem,5vw,3.5rem)] lg:grid-cols-12">
          {/* ПОЛОЖЕНИЕ ПОЛОВИН — 1–5 и 8–12, по краям полосы, две свободные
              колонки между ними. Ф40 просила сдвинуть обе к центру (2–6 и
              7–11), и это было сделано; следующим сообщением — «нет нет, два
              абзаца из как получается кино верни обратно». Возвращено ровно
              исходное положение. Оставлено записью, чтобы правка не была
              предложена снова как «недоделанная». */}
          <div className="reveal lg:col-span-5 lg:col-start-1">
            <p className="t-quote max-w-[30ch]">{copy.cinema.a.text}</p>
          </div>
          {/* Ф40: «второй абзац справа сделай по правому краю». Выключка
              вправо у правой половины диптиха — тот же приём, что уже стоит
              у плёночной карточки в услугах: половина прижата к правому
              краю полосы, и её левый край висел в воздухе. Теперь у пары
              общий внешний контур — левая выключена влево, правая вправо, —
              а «рваной» остаётся только середина, где паре и положено
              расходиться. `ml-auto` нужен потому, что мера в 30 знаков уже
              колонки: без него выключились бы строки внутри абзаца, а сам
              абзац остался бы у левого края своей колонки. */}
          <div
            className="reveal lg:col-span-5 lg:col-start-8 lg:mt-[clamp(1.5rem,4.5vh,4rem)]"
            style={{ ['--i' as string]: 1 }}
          >
            {/* Ф52: выключка вправо теперь на ВСЕХ ширинах, не только с `lg:`.
                Ф40 ограничила её широким экраном, рассудив, что на телефоне
                половины идут друг под другом и «правой» среди них нет. На деле
                вторая половина и на узком остаётся ответом первой — пара
                читается как реплика и отклик, и разное равнение держит эту
                пару даже в столбик. */}
            <p className="t-quote ml-auto max-w-[30ch] text-right">{copy.cinema.b.text}</p>
          </div>
        </div>
      </div>
    </section>
  );
}

/* ═════════════════════════ ШЕСТЬ КАДРОВ ═════════════════════════
   Р4: кадр — объект, не фон. Поэтому не ровная сетка 3×2, а разнобой по ширине
   и по вертикали: каждый кадр стоит в своём воздухе и подписан. Приём из
   pho-anothermag3 — кадр и текст как два равных объекта. */
const GRID_LAYOUT = [
  { col: 'lg:col-span-4 lg:col-start-1', push: '', size: 'w-[86%]' },
  { col: 'lg:col-span-5 lg:col-start-7', push: 'lg:mt-[7rem]', size: 'w-full' },
  { col: 'lg:col-span-4 lg:col-start-3', push: 'lg:mt-[3rem]', size: 'w-[78%] ml-auto' },
  { col: 'lg:col-span-3 lg:col-start-8', push: 'lg:mt-[1rem]', size: 'w-[72%]' },
  { col: 'lg:col-span-4 lg:col-start-2', push: 'lg:mt-[2rem]', size: 'w-[84%]' },
  { col: 'lg:col-span-5 lg:col-start-7', push: 'lg:mt-[5rem]', size: 'w-full' },
];

/* ВЫКЛЮЧЕНО Ф28 — секция не рендерится (`Page` выше). Компонент экспортируется,
   а не удаляется и не остаётся молча неиспользованным: экспорт — это подпись
   «оставлено намеренно», а безымянная неиспользуемая функция в файле выглядит
   забытым мусором и будет вычищена следующим, кто сюда придёт. Владелица
   сказала «добавим позже», значит возврат — одна строка в `Page`. */
export function Grid() {
  return (
    <section id="kadry" className={`${SECT} pb-[clamp(4rem,12vh,9rem)]`}>
      <div className={WRAP}>
        <h2 className="reveal t-h2">{copy.grid.title}</h2>

        <div className="mt-[clamp(2.5rem,7vh,5rem)] grid grid-cols-1 gap-x-[var(--gap)] gap-y-[clamp(3rem,9vh,7rem)] lg:grid-cols-12">
          {copy.grid.frames.map((f, i) => {
            const L = GRID_LAYOUT[i];
            return (
              <figure
                key={f.src}
                className={`m-0 ${L.col} ${L.push} ${i % 2 ? 'ml-auto w-[82%] sm:w-auto' : 'w-[94%] sm:w-auto'}`}
              >
                <div data-focus="in" className="overflow-hidden">
                  <img
                    src={f.src}
                    width={1200}
                    height={1600}
                    alt={f.alt}
                    className={`frame block ${L.size}`}
                    loading="lazy"
                    decoding="async"
                  />
                </div>
                <figcaption className="t-mono mt-[0.9rem] text-[color:var(--ink-mute)]">
                  {f.caption}
                </figcaption>
              </figure>
            );
          })}
        </div>

        <p className="reveal mt-[clamp(3rem,8vh,6rem)]">
          <a className="link-major t-h3" href="/#kadry">
            {copy.grid.link}
          </a>
        </p>
      </div>
    </section>
  );
}

/* ═════════════════════════ ЧТО МОЖНО СНЯТЬ ═════════════════════════
   Самая трудная зона главной: три предложения с ценами и один ответ FAQ
   на 72 слова ниже. Прайс таблицей здесь запрещён (`PRICE-POSITION.md`):
   таблица ставит автора в один ряд с теми, кто стоит 3 000 ₽.

   Запрет лока «плашка цены с изображением-заглушкой» исполнен так: цена стоит
   на цветовой плашке темы, а фотография есть только там, где она настоящая, —
   три кадра из портфолио по Ф26.

   Творческая съёмка выделена АСИММЕТРИЕЙ перечисления, а не размером ячейки
   (композиционный контракт COPY § 13 п. 4): текст уходит на другую сторону
   разворота, и она стоит после волосяной линейки — то есть не третья ячейка
   одинакового ряда, а отдельный предмет.

   РЕДАКЦИЯ 2 (Ф28), три правки в этой секции:

   1. ПОКАЗ КАДРОВ УМЕНЬШЕН. «уменьши их в размере, чтобы не было так заметно
      ухудшение качества». Было: кадр на всю ширину своей колонки, то есть до
      700 px на 1920 при исходнике 853×1280 — компрессия портфолио-копий
      (`PHOTO-VERDICT.md`) на таком размере видна прямо. Стало: явный потолок
      ширины на каждом кадре, 15–19 rem. Это ровно вывод вердикта: мельче
      показ — меньше видна компрессия. Разница размеров между кадрами
      сохранена, иначе четыре одинаковых кадра сложились бы в сетку карточек.

   2. АСИММЕТРИЯ ТВОРЧЕСКОЙ ПЕРЕЕХАЛА С ПРОПОРЦИИ НА ПОЛОЖЕНИЕ. Раньше её
      кадр был горизонтальным (3:2) и этим отличался от двух вертикальных.
      Новый файл Ф28 `IMG_6281.JPG` — вертикальный портрет в полный рост, как
      и остальные три: кроп 3:2 отрезал бы фигуре голову. Поэтому пропорция у
      всех четырёх одна, а творческая выделяется тем, что стоит одна на
      развороте после линейки и её кадр самый крупный из четырёх.

   3. ПОЯВИЛСЯ СЛОТ «ПЛЁНКА» — «для услуги на плёнку добавь IMG_1651». Стоит
      там, где про плёнку до сих пор был только текст (`home:offer.note`).
      Своего заголовка у слота нет: подписи «Плёнка» в `COPY.md` не существует,
      а сочинять экранный текст билдеру запрещено.

   РЕДАКЦИЯ 5 (Ф36 п.8), четыре правки поверх редакции 2:

   4. ВЕРСТКА ПЛОТНЕЕ, ФОТО РЯДОМ С ТЕКСТОМ, НЕ НАД НИМ. Дословно: «не делай
      такого большого разрыва между фотографиями, где услуги. я представляю
      как услуги описаны справа от фотографии». `OfferRow` (индивидуальная,
      парная) была вертикальным стеком — фото сверху, текст ниже с зазором.
      Стала горизонтальной: фото слева, текст справа, с `sm:` — на 360 px
      узкому экрану оставлен вертикальный стек с тем же тесным зазором:
      боковая раскладка при 128 px кадра и русских слов на 324 px рабочей
      ширины сжала бы текст до нечитаемого столбика (правило «очень длинное
      русское слово на 360px»). Кадры при этом мельче прежних (15/13 rem →
      9/8 rem): при боковой раскладке крупный кадр столкнул бы текст за край
      колонки.

   5. ЧЕТВЁРТАЯ КАРТОЧКА «ПЛЁНОЧНАЯ». Прежний безымянный слот «плёнка» (Ф28,
      текст `offer.note` рядом) стал полноценной карточкой — своё имя, своя
      подпись (дословно владелицы, `copy.offer.film.caption`), тот же
      зеркальный разворот к творческой (там кадр слева текст справа, здесь
      наоборот) — четыре карточки, а не три с довеском.

   6. ФОРМАТ ЦЕНЫ «ГОРОД — ЦЕНА/ЧАС». См. `copy.ts`, комментарий у `offer`.

   7. ССЫЛКА «ВЕСЬ ПРАЙС И УСЛОВИЯ» СНЯТА ЦЕЛИКОМ — не рендерится, зона
      `copy.offer.cta` сохранена дормантной (тот же принцип, что `home:grid`,
      Ф28). */
function Offer() {
  const c = copy.offer.cards;
  return (
    <section id="uslugi" className={`${SECT} scroll-mt-[5rem] py-[clamp(4rem,12vh,9rem)]`}>
      <Stars count={11} seed={37} scale="fine" tones={[1, 0]} />

      <div className={WRAP}>
        <div className="grid grid-cols-1 gap-x-[var(--gap)] gap-y-[clamp(1.4rem,3vw,2.4rem)] lg:grid-cols-12">
          <h2 className="reveal t-h1 lg:col-span-6 lg:col-start-1">{copy.offer.title}</h2>
          <p
            className="reveal t-lead max-w-[42ch] text-[color:var(--ink-soft)] lg:col-span-5 lg:col-start-8 lg:mt-[0.6rem]"
            style={{ ['--i' as string]: 1 }}
          >
            {copy.offer.lead}
          </p>
        </div>

        {/* два по запросу. Кадры разного размера намеренно — ряд из двух
            одинаковых читался бы как пара карточек.
            Ф36 п.8 уменьшила их до 9/8 rem, когда фото переехало вбок от
            текста. Ф39 п.5 отменяет это уменьшение: «фотографии рядом с
            услугами сделай больше размером все-таки» — 14 и 12 rem, плюс
            половина роста. Разница между кадрами сохранена.
            Правая карточка расширена с 5 до 6 колонок и подвинута с 8-й на
            7-ю: при кадре в 12 rem пятиколоночная ячейка оставляла тексту
            меньше меры, чем «Рекомендуемая длительность» требует, и он
            сыпался в столбик.

            Ф40: «фотографии у индивидуальной и парной сделай одного
            размера» — 14 и 12 rem сведены к 14 у обеих. Прежнее правило
            («ряд из двух одинаковых кадров читается как пара карточек»)
            владелицей отменено прямо; асимметрию ряда теперь держит
            единолично сдвиг правой карточки вниз (`md:mt-…`), и её хватает:
            карточки не стоят на одной линии, значит и на пару одинаковых
            ячеек не похожи. */}
        {/* Ф52: «расстояние между текстом "на ваш выбор классика и
            творчество…" и фотографией из индивидуальной сделай меньше, чтобы
            не бросался в глаза такой большой интервал». Было
            clamp(3rem, 9vh, 6rem) — до 96 px на высоком экране, и вводная
            строка отрывалась от карточек, к которым она относится. Стало
            clamp(1.6rem, 4.5vh, 3rem), вдвое меньше по всей вилке. Зазор
            МЕЖДУ карточками (`gap-y`) не тронут: там разрыв держит их
            раздельность, и он про другое. */}
        <div className="mt-[clamp(1.6rem,4.5vh,3rem)] grid grid-cols-1 gap-x-[var(--gap)] gap-y-[clamp(3rem,8vw,5rem)] md:grid-cols-12">
          <OfferRow i={0} card={c.portrait} cls="md:col-span-6 md:col-start-1" frame="max-w-[14rem]" />
          <OfferRow
            i={1}
            card={c.duo}
            cls="md:col-span-6 md:col-start-7 md:mt-[clamp(2rem,6vh,4.5rem)]"
            frame="max-w-[14rem]"
            mobileRight
          />
        </div>

        <div className="rule mt-[clamp(3rem,8vh,5.5rem)]" />

        {/* третья, без запроса: разворот, а не ячейка.

            Ф39 п.6: «услуги творческая и пленочная сделай компактнее. мне не
            нравится что между текстом и фотографией такое большое расстояние.
            фотографию у творческой чуть уменьши».

            ОТКУДА БРАЛОСЬ РАССТОЯНИЕ — это была не величина зазора, а сетка.
            Кадр в 19 rem (304 px) стоял в пятиколоночной ячейке шириной ~590 px
            на 1500-й мере, текст начинался с 7-й колонки: между краем
            фотографии и первой буквой набегало ~340 px пустоты, из которых
            почти всё — недобранная ширина ячейки, а не осознанный зазор.
            Правка убирает саму сетку у этих двух разворотов: `flex`, кадр и
            текст соседи, зазор задан числом и виден в коде. Разворотами они
            быть не перестали — ширина блока ограничена, и творческая прижата
            влево, а плёночная вправо (`ml-auto` ниже), так что чередование
            сторон, ради которого сетка и стояла, сохранено.

            Кадр 19 → 16 rem («чуть уменьши»). */}
        {/* Ф40: «в творческой добавь побольше фотографий и сделай анимацию
            веером, как в интерактиве придумать съемку». Одиночный кадр в
            `TiltFrame` заменён на веер из шести (`Fan.tsx` — тот же
            компонент, что на экране результата раскадровки, вариант
            `compact`; кадры и порядок — `copy.ts`).

            ТИЛТ ПО КУРСОРУ СНЯТ С ЭТОЙ КАРТОЧКИ. Не забыт: у веера курсор
            уже занят — он разворачивает стопку в ряд и увеличивает кадр под
            указателем. Наклон плоскости поверх этого был бы вторым ответом
            на то же движение мыши, и оба читались бы хуже. У остальных трёх
            карточек `TiltFrame` на месте.

            Блок стал шире (54 → 64 rem): развёрнутому ряду из шести кадров
            нужно место, иначе он ушёл бы в собственную прокрутку. Кадр и
            текст при этом остались соседями — то, ради чего Ф39 п.6 убирала
            отсюда сетку, не потеряно. */}
        {/* Порог раскладки у ЭТИХ ДВУХ карточек — `lg:` (1024), а не `sm:`
            (640), как у первых двух. Замерено: на 768 px боковая раскладка
            оставляла вееру 310 px, и развёрнутый ряд из шести вылезал на
            123 px — то есть на планшете «раскрылся в ряд» снова означало бы
            «две карточки под обрезом». Ниже 1024 карточка складывается в
            столбик, и веер получает всю ширину полосы (687 px на 768) —
            там помещается девять кадров, запас есть. У первых двух карточек
            порог остаётся `sm:`: там одиночный кадр, ему хватает и 128 px. */}
        <div className="offer-card mt-[clamp(3rem,8vh,5.5rem)] flex max-w-[64rem] flex-col gap-y-[clamp(1.2rem,3vw,1.8rem)] lg:flex-row lg:items-center lg:gap-x-[clamp(1.6rem,3.4vw,2.6rem)] lg:gap-y-0">
          {/* `grow` у веера и фиксированная доля у текста (ниже) — не
              косметика, а условие, при котором развёрнутый ряд помещается.
              Пока обе половины делили место «по содержимому», ширина
              контейнера веера зависела от самого веера: он раскрывался,
              просил больше, получал меньше, и последняя карточка уезжала
              под обрез (замерено `scrollWidth` против `clientWidth`: 561
              против 533). Теперь текст занимает ровно свою долю, а веер —
              весь остаток, и остаток известен заранее, до раскрытия. */}
          <div className="reveal min-w-0 grow">
            <Fan
              variant="compact"
              photos={c.idea.photos.map((p) => ({
                key: p.src,
                src: p.src,
                /* Крупная копия для просмотра по нажатию (Ф60). Имя выводится,
                   а не хранится в COPY: экспорт заводит кадры вееров ТОЛЬКО
                   парой (`веер()` в `export-photos.mjs`), поэтому у каждой
                   миниатюры есть сосед с тем же именем и суффиксом `-full`.
                   Держать второй путь в тексте значило бы дублировать это
                   правило в девяти местах и ждать, пока одно разъедется. */
                full: p.src.replace(/\.webp$/, '-full.webp'),
                alt: p.alt,
                w: 700,
                h: 933,
              }))}
              label={`Кадры творческих съёмок, ${c.idea.photos.length} штук — наведите курсор или сфокусируйте, чтобы увидеть все целиком`}
            />
          </div>
          <div className="min-w-0 lg:shrink-0 lg:basis-[21rem]">
            <h3 className="reveal t-h2">{c.idea.name}</h3>
            {/* тот же схлопываемый слот, что у первых двух карточек (Ф40) */}
            <div className="offer-price">
              <div>
                <p
                  className="reveal mb-[clamp(0.8rem,2vw,1.2rem)] mt-[clamp(0.7rem,1.8vw,1.1rem)]"
                  style={{ ['--i' as string]: 1 }}
                >
                  <PricePlate text={c.idea.price} />
                </p>
              </div>
            </div>
            {/* Ф41: «чуть увеличить интервал у творческой между названием
                услуги и текстом. сделай его таким же, как у остальных
                услуг». Отступа не было вовсе: он снялся вместе с сеткой в
                Ф39 п.6 («сделай компактнее») и стал не нужен, пока плашка
                цены стояла между именем и текстом и держала зазор собой.
                Ф40 схлопнула место под плашку в покое — и имя услуги
                слиплось с описанием, чего у первых двух карточек не
                случилось: там у описания свой `mt-[0.7rem]`. Здесь ровно то
                же число, не «похожее»: просили как у остальных. */}
            <p
              className="reveal t-body mt-[0.7rem] max-w-[38ch] text-[color:var(--ink-soft)]"
              style={{ ['--i' as string]: 2 }}
            >
              {c.idea.includes}
            </p>
          </div>
        </div>

        <div className="rule mt-[clamp(3rem,8vh,5.5rem)]" />

        {/* ПЛЁНОЧНАЯ (Ф36 п.8) — четвёртая карточка, не безымянная доплата:
            своё имя и своя подпись (`copy.offer.film.name`/`.caption`),
            заменяет прежний текст `offer.note` (см. комментарий у `offer`,
            `copy.ts`). Кадр здесь мельче всех, 12 rem. Зеркальный разворот к
            творческой: там кадр слева, текст справа, здесь наоборот. Ряда из
            четырёх одинаковых ячеек в секции так и не появляется. */}
        {/* Ф39 п.6, вторая половина — та же правка, что у творческой: сетка
            снята, кадр и текст стоят соседями. Здесь пустоты было ещё больше:
            текст занимал колонки 1–6, кадр в 12 rem висел в четырёхколоночной
            ячейке с 9-й, прижатый к правому краю, — между ними получалось
            больше половины меры. Блок прижат вправо (`ml-auto`), чтобы
            зеркальность к творческой не потерялась вместе с сеткой.
            Кадр 12 → 14 rem («фотографию у пленочной наоборот чуть
            увеличь»); он по-прежнему мельче кадра творческой (16 rem), как и
            был, — увеличены оба конца, порядок не перевернулся.
            `sm:flex-row-reverse` — кадр справа, текст слева: та же сторона,
            что и до правки, читательский порядок в DOM (сначала имя услуги,
            потом фотография) при этом сохранён. */}
        {/* Порог `lg:`, как у творческой, и по той же измеренной причине. */}
        <div className="offer-card mt-[clamp(3rem,8vh,5.5rem)] ml-auto flex max-w-[64rem] flex-col gap-y-[clamp(1.2rem,3vw,1.8rem)] lg:flex-row-reverse lg:items-center lg:gap-x-[clamp(1.6rem,3.4vw,2.6rem)] lg:gap-y-0">
          {/* Ф40: «в пленочной сделай такой же веер» — одиночный кадр заменён
              веером из пяти (`Fan.tsx`, вариант `compact`, кадры в
              `copy.ts`). Доли те же, что у творческой, и по той же
              измеренной причине: текст занимает свою, веер забирает
              остаток. Блок расширен 54 → 64 rem, как и там.

              СНЯТЫ ВМЕСТЕ С ОДИНОЧНЫМ КАДРОМ, оба намеренно:
               · `TiltFrame` — у веера курсор уже занят разворотом стопки,
                 наклон был бы вторым ответом на то же движение мыши (то же
                 решение, что в творческой);
               · `FilmCaption` — EXIF-подпись «плёнка» (Ф29 п.5) висела на
                 этом кадре ровно потому, что он был ЕДИНСТВЕННЫМ
                 подтверждённым плёночным на сайте. Теперь плёночные все
                 пять, и подпись на каждой карточке веера стала бы пятью
                 одинаковыми ярлыками — шум вместо детали. Компонент
                 `FilmCaption.tsx` не удалён, возврат — одна обёртка. То,
                 что это плёнка, карточка и так говорит именем и текстом. */}
          <div className="reveal min-w-0 grow" style={{ ['--i' as string]: 1 }}>
            <Fan
              variant="compact"
              photos={copy.offer.film.photos.map((p) => ({
                key: p.src,
                src: p.src,
                /* Крупная копия для просмотра по нажатию (Ф60). Имя выводится,
                   а не хранится в COPY: экспорт заводит кадры вееров ТОЛЬКО
                   парой (`веер()` в `export-photos.mjs`), поэтому у каждой
                   миниатюры есть сосед с тем же именем и суффиксом `-full`.
                   Держать второй путь в тексте значило бы дублировать это
                   правило в девяти местах и ждать, пока одно разъедется. */
                full: p.src.replace(/\.webp$/, '-full.webp'),
                alt: p.alt,
                w: 700,
                h: 933,
              }))}
              label={`Кадры плёночных съёмок, ${copy.offer.film.photos.length} штук — наведите курсор или сфокусируйте, чтобы увидеть все целиком`}
            />
          </div>
          {/* Ф40: «текст у пленочной сделай по правому краю». Выключка вправо
              здесь не украшение и не случайность в ряду других карточек: это
              единственный разворот, где кадр стоит СПРАВА от текста
              (`sm:flex-row-reverse` выше), и левый край текста упирается в
              пустоту, а правый — в фотографию. Прижав строки к правому краю,
              текст получает общую вертикаль с кадром. `ml-auto` на самом
              блоке нужен потому, что мера в 46 знаков уже колонки: без него
              выключка сдвинула бы только строки внутри блока, а сам блок
              остался бы слева. */}
          <div className="reveal min-w-0 text-right lg:shrink-0 lg:basis-[21rem]">
            <h3 className="t-h3">{copy.offer.film.name}</h3>
            <p className="t-body ml-auto mt-[clamp(0.8rem,2vw,1.2rem)] max-w-[46ch] text-[color:var(--ink-soft)]">
              {copy.offer.film.caption}
            </p>
            {/* Ф41: «в пленочной сделай то же самое с ценами, что и со всеми
                услугами» — та же плашка и та же механика (скрыта в покое,
                возникает по наведению на карточку, место под неё
                схлопывается), см. `.offer-price` в `styles.css`.
                ПОЛОЖЕНИЕ ОТЛИЧАЕТСЯ ОТ ТРЁХ ДРУГИХ КАРТОЧЕК: там плашка
                стоит между именем услуги и описанием, здесь — ПОСЛЕ
                описания. Так просила владелица дословно: «…(рекомендуемая
                длительность…). (затем блок с ценами и анимацией)». Порядок
                её, не мой; механика от этого не меняется — слот раскрывается
                вниз, а не вверх, и описание остаётся на месте. */}
            <div className="offer-price">
              <div>
                <p className="mt-[clamp(0.7rem,1.8vw,1.1rem)]">
                  <PricePlate text={copy.offer.film.price} />
                </p>
              </div>
            </div>
          </div>
        </div>

        {/* «Весь прайс и условия» — ВЫКЛЮЧЕНО Ф36 п.8: «Убрать: ссылку...
            под карточками услуг — снимается целиком». Зона `copy.offer.cta`
            сохранена дормантной, ссылка не рендерится. */}
      </div>
    </section>
  );
}

/* РЕДАКЦИЯ 5 (Ф36 п.8): «я представляю как услуги описаны справа от
   фотографии» — было вертикальным стеком (фото сверху, текст под ним с
   зазором), стало горизонтальной парой на `sm:` и шире. На 360 px раскладка
   остаётся вертикальной (`flex-col`): боковая раскладка при 128 px кадра и
   узкой рабочей ширине сжала бы текст до нечитаемого столбика — правило про
   очень длинное русское слово на 360px. Зазор в обоих случаях узкий (в
   отличие от прежнего `mt-[clamp(1.1rem,2.4vw,1.6rem)]` перед заголовком):
   ровно то, о чём просила владелица («не делай такого большого разрыва»). */
function OfferRow({
  i,
  card,
  cls,
  frame,
  /** Ф50: «хочу чтобы услуга парной съёмки в мобильной версии была по
   *  правому краю, как плёночная». Только на узком экране — на широком
   *  карточка стоит в правой колонке сетки, и там равнение и так справа
   *  по построению. */
  mobileRight = false,
}: {
  i: number;
  card: { name: string; price: string; includes: string; photo: string; alt: string };
  cls: string;
  mobileRight?: boolean;
  /** потолок ширины кадра — Ф28 «уменьши их в размере», Ф36 п.8 уменьшено ещё */
  frame: string;
}) {
  return (
    /* `offer-card` — область наведения плашки цены (Ф40, см. `styles.css`).
       Стоит на внешнем узле карточки, а не на внутреннем ряду: наводить
       владелица просит «на тип съемки/услуги», то есть на карточку целиком,
       включая заголовок и описание, а не только на полосу «фото + текст». */
    <div className={`offer-card reveal ${cls}`} style={{ ['--i' as string]: i }}>
      <div
        className={`flex flex-col gap-y-[0.8rem] sm:flex-row sm:items-start sm:gap-x-[clamp(1rem,2.6vw,1.6rem)] sm:gap-y-0 ${
          mobileRight ? 'text-right sm:text-left' : ''
        }`}
      >
        <figure className="m-0 shrink-0">
          {/* Ф50: кадр прижимается вправо ИМЕННО здесь, а не выравниванием
              всей колонки (`items-end`). Колонка с `items-end` сжимается до
              содержимого, и пока ленивая картинка не загрузилась, ширина
              `<figure>` равна нулю — замерено: 0 px вместо 224. Отступ на
              самом кадре двигает его вправо, не трогая размеры колонки. */}
          <TiltFrame className={`overflow-hidden ${frame} ${mobileRight ? 'ml-auto sm:ml-0' : ''}`}>
            <div data-focus="in">
              <img
                src={card.photo}
                width={1000}
                height={1333}
                alt={card.alt}
                className="frame block w-full"
                loading="lazy"
                decoding="async"
              />
            </div>
          </TiltFrame>
        </figure>
        <div className="min-w-0">
          <h3 className="t-h3">{card.name}</h3>
          {/* Схлопываемое место под цену (Ф40) — в покое высота ноль, и
              описание стоит вплотную под именем услуги, «вместо цен»; по
              наведению строка раскрывается и описание съезжает вниз.
              Механика — `.offer-price` в `styles.css`. Отступы плашки
              стоят внутри обёртки, чтобы исчезать вместе с ней. */}
          <div className="offer-price">
            <div>
              <p className="mb-[0.7rem] mt-[0.6rem]">
                <PricePlate text={card.price} />
              </p>
            </div>
          </div>
          <p
            className={`t-body mt-[0.7rem] max-w-[28ch] text-[color:var(--ink-soft)] ${
              mobileRight ? 'ml-auto sm:ml-0' : ''
            }`}
          >
            {card.includes}
          </p>
        </div>
      </div>
    </div>
  );
}

/* ═════════════════════════ ПРИДУМАТЬ СЪЁМКУ ═════════════════════════
   Ф63. Владелица: «раздел "придумать съемку" прям спрятался и его не сразу
   может заметить пользователь… поиск этого раздела тоже выглядит как своего
   рода интерактив и путешествие по сайту, но как продукт не сразу достигает
   своего клиента».

   ДИАГНОЗ, с которым она согласилась: на раздел показывали ДВА указателя —
   пункт шапки и стрелка «Загляните сюда» (`NavHint.tsx`), — и оба служебные.
   Раздел себя НАЗЫВАЛ, но ни разу не ПОКАЗЫВАЛ. Указатель не лечит того, что
   нечего показать.

   ЧТО ЗДЕСЬ СТОИТ — не ссылка и не карточка-анонс, а НАСТОЯЩИЙ ПЕРВЫЙ ВОПРОС
   квиза с настоящими его плитками (`storyboard/q0.ts`, тот же массив, что
   строит дерево на второй странице). Нажатие уводит на `storyboard.html` с
   уже отмеченным ответом, и там задаётся сразу ВТОРОЙ вопрос — первый уже
   стоит в лестнице отвеченным. Человек не «переходит в раздел, чтобы начать»,
   он уже начал; начатое хочется закончить.

   ПЛИТКИ — ССЫЛКИ, А НЕ КНОПКИ. Средняя кнопка мыши, «открыть в новой
   вкладке» и просто предпросмотр адреса в строке состояния работают сами
   собой, без единой строки обработчиков. Ни одного состояния квиза на
   главной не заводится: главная спрашивает, отвечает страница фичи.

   МЕСТО — сразу после услуг, дословно по её указанию («поставить раздел
   "придумать съемку" после раздела "что снимаю"»). Оно же самое верное по
   смыслу: читатель только что посмотрел четыре формата с ценами и думает «а
   мне какая?» — это и есть точка сомнения, которую интерактив снимает.

   ПУТЕШЕСТВИЕ НЕ ПОТЕРЯНО: пункт в шапке и стрелка-подсказка остались на
   месте, интерактив остался интерактивом. Изменилось одно — вход в него
   стоит на дороге, а не за дверью.

   ВЕСЬ ТЕКСТ ЗДЕСЬ — ЕЁ, до знака: надзаголовок, заголовок и подводка из
   зоны `copy.storyboard` (переехали из `StoryboardApp.tsx`, см. комментарий
   у зоны), вопрос и подписи плиток — из дерева Ф33. Билдерского текста в
   блоке ровно одна строка — служебное «Вопрос 1», и она не сочинена здесь:
   тем же словом и тем же начертанием (`t-mono`) нумерует шаги сам квиз
   (`QuestionLadder.tsx`). Числа «из скольких» нет намеренно — до ответа на
   первый вопрос длина пути неизвестна (ветка «вайб» даёт 5 шагов или 6), и
   обещать точное число значило бы соврать. */
function Storyboard() {
  return (
    <section id="raskadrovka" className={`${SECT} scroll-mt-[5rem] py-[clamp(4rem,12vh,9rem)]`}>
      {/* Тона с перевесом в жёлтый — здесь начинается разгон к низу страницы
          (Ф39 п.8): выше, в услугах, россыпь мельче и холоднее, ниже, в
          «С чего начать», её уже тринадцать. Ранг `fine`: под звёздами
          вопрос и плитки, крупные спорили бы с ними за внимание. */}
      <Stars count={11} seed={83} scale="fine" tones={[1, 0]} />

      <div className={WRAP}>
        <div className="grid grid-cols-1 gap-x-[var(--gap)] gap-y-[clamp(1.4rem,3vw,2.4rem)] lg:grid-cols-12">
          {/* НАДЗАГОЛОВОК «ПРИДУМАТЬ СЪЁМКУ» СНЯТ (Ф64). Владелица: «перед
              названием "соберем для вас референсы к нашей съемке" убери
              название раздела "придумать съемку", пусть будут все названия в
              единой стилистике».
              Он стоял здесь ради узнавания: та же строка и то же начертание,
              что у `h1` страницы фичи. Но ни одна другая секция главной
              надзаголовка не имеет — ни услуги, ни «Кто снимает», ни «С чего
              начать», — и один блок с двухэтажной шапкой выбивался из ряда.
              Единая стилистика оказалась дороже узнавания. */}
          <h2 className="reveal t-h1 lg:col-span-6 lg:col-start-1">{copy.storyboard.title}</h2>
          <p
            className="reveal t-lead max-w-[42ch] text-[color:var(--ink-soft)] lg:col-span-5 lg:col-start-8 lg:mt-[0.6rem]"
            style={{ ['--i' as string]: 1 }}
          >
            {copy.storyboard.lead}
          </p>
        </div>

        {/* Ступени входа пересчитаны Ф64 (было 1–2–3): вместе с надзаголовком
            ушла и его ступень, а дырка в лестнице задержек — это пауза на
            пустом месте, которую видно глазом. */}
        <div className="reveal mt-[clamp(2rem,5vh,3.4rem)]" style={{ ['--i' as string]: 2 }}>
          <p className="t-mono text-[color:var(--ink-mute)]">Вопрос 1</p>
          <h3 className="t-h3 mt-[0.6rem] max-w-[20ch]">{Q0_TITLE}</h3>
          <div className="sb-tiles mt-[clamp(1.4rem,3.6vw,2.2rem)]">
            {Q0_TILES.map((t) => (
              <a
                key={t.value}
                className="sb-tile"
                href={`/storyboard.html?${POVOD_PARAM}=${POVOD_SLUG[t.value]}`}
              >
                {t.label}
              </a>
            ))}
          </div>
        </div>
      </div>
    </section>
  );
}

/* ═════════════════════════ КАК ЭТО УСТРОЕНО ═════════════════════════
   Лестница из четырёх шагов с волосяными линейками, а не четыре карточки:
   шаги идут во времени, и последовательность обязана быть видна глазом.
   Рядом с четвёртым шагом («Отдаю кадры») стоит демонстрация отдачи —
   она иллюстрирует именно его (COPY § 1.6.1).

   `id="podgotovka"` (редакция 5) — раньше этот якорь принадлежал `Faq()`.
   Ф36 п.1 переименовала пункт навигации «Подготовка к съёмке» → «С чего
   начать», Ф37 п.4 следом убрала саму секцию FAQ целиком — без переноса
   якоря ссылка вела бы в никуда. Новое имя пункта дословно совпадает с
   началом нового заголовка этой секции (Ф36 п.9): «С чего начать» здесь и
   есть. См. комментарий у `NAV` в шапке и README § 10. */
function How() {
  return (
    <section id="podgotovka" className={`${SECT} scroll-mt-[5rem] py-[clamp(4rem,12vh,9rem)]`}>
      {/* Ф39 п.8 — «добавь ближе к низу побольше желтых звездочек». Это
          предпоследняя секция страницы, и разгон начинается с неё: число
          8 → 13, тона с перевесом в жёлтый (повтор значения = вес, см.
          `StarsProps.tones`). Ранг остаётся `fine`: здесь под звёздами
          лестница шагов и демонстрация папок, крупные спорили бы с ними за
          внимание. Самый плотный жёлтый — ниже, в блоке контактов. */}
      <Stars count={13} seed={53} scale="fine" tones={[1, 1, 0]} />

      <div className={WRAP}>
        <h2 className="reveal t-h2 max-w-[26ch]">{copy.how.title}</h2>

        <div className="mt-[clamp(2.5rem,7vh,4.5rem)] grid grid-cols-1 gap-x-[var(--gap)] lg:grid-cols-12">
          <ol className="m-0 list-none p-0 lg:col-span-7 lg:col-start-1">
            {copy.how.steps.map((s, i) => (
              <li
                key={s.label}
                className="reveal grid grid-cols-[3.2rem_1fr] gap-x-[clamp(0.8rem,2vw,1.6rem)] border-t border-[color:color-mix(in_srgb,var(--ink)_16%,transparent)] py-[clamp(1.4rem,3.5vh,2.4rem)]"
                style={{ ['--i' as string]: i % 2 }}
              >
                <p className="t-mono m-0 pt-[0.3em] text-[color:var(--accent)]">
                  {String(i + 1).padStart(2, '0')}
                </p>
                <div>
                  <h3 className="t-h3 m-0">{s.label}</h3>
                  <p className="t-body mt-[0.6rem] max-w-[42ch] text-[color:var(--ink-soft)]">
                    {s.text}
                  </p>
                </div>
              </li>
            ))}
          </ol>

          <div className="reveal mt-[clamp(2.6rem,7vh,0rem)] lg:col-span-4 lg:col-start-9 lg:mt-[clamp(6rem,18vh,12rem)]">
            <DeliveryDemo
              trigger={copy.delivery.trigger}
              digital={copy.delivery.digital}
              film={copy.delivery.film}
              note={copy.delivery.note}
            />
          </div>
        </div>
      </div>
    </section>
  );
}

/* ═════════════════════════ ПОМОГУ ПОДГОТОВИТЬСЯ ═════════════════════════
   Самая плотная зона главной: четыре ответа, один из них на 72 слова (BC-04).
   Никаких раскрывашек: ответ, который надо открыть, на сайте с ценой выше
   рынка читается как то, что прячут. Вопрос крупно слева, ответ узкой мерой
   справа — здесь и проверяется, держит ли направление плотность.

   ВЫКЛЮЧЕНО Ф37 п.4 — не вызывается из `Page()`, см. комментарий там и
   README § 10. Компонент сохранён экспортируемым по тому же принципу, что
   `Grid()` (Ф28): возврат — одна строка. `id="podgotovka"` СНЯТ с этой
   секции при выключении: якорь переехал на `How()` (Ф36 п.9), и держать
   тот же `id` здесь было бы миной — при случайном одновременном возврате
   обоих компонентов на странице оказались бы два узла с одним `id`,
   а такой дубль не виден на кадре, только замером
   (`document.querySelectorAll('#podgotovka').length`). */
export function Faq() {
  return (
    <section className={`${SECT} py-[clamp(4rem,12vh,9rem)]`}>
      <div className={WRAP}>
        <h2 className="reveal t-h2 max-w-[22ch]">{copy.faq.title}</h2>

        <dl className="mt-[clamp(2.5rem,7vh,5rem)]">
          {copy.faq.items.map((item, i) => (
            <div
              key={item.q}
              className="reveal grid grid-cols-1 gap-x-[var(--gap)] gap-y-[clamp(0.8rem,2vw,1.3rem)] border-t border-[color:color-mix(in_srgb,var(--ink)_16%,transparent)] py-[clamp(1.6rem,4.5vh,3rem)] lg:grid-cols-12"
              style={{ ['--i' as string]: i % 2 }}
            >
              <dt className="t-h3 lg:col-span-5 lg:col-start-1">{item.q}</dt>
              <dd className="t-body m-0 max-w-[56ch] text-[color:var(--ink-soft)] lg:col-span-6 lg:col-start-7">
                {item.a}
              </dd>
            </div>
          ))}
        </dl>
      </div>
    </section>
  );
}

/* ═════════════════════════ КТО СНИМАЕТ ═════════════════════════
   `#o-mne` — переходный адрес пункта «Обо мне» из навигации Ф28: страницы
   «Обо мне» ещё нет, эта секция — её анонс на главной.

   Ф39, две правки:

   п.3 ФОТОГРАФИЯ АВТОРА СЛЕВА. «в раздел "обо мне" слева добавь мою
   фотографию» — кадр встал в левую колонку под заголовок, текст ушёл в
   правую. Заголовок с места не двигался: он и раньше стоял слева, теперь
   просто не один. Единственная горизонталь (3:2) на всей странице — на
   рэке и в услугах всё вертикальное; раздел про автора отличается от них
   не только текстом. Обоснование пропорции — `export-photos.mjs`.

   Правая колонка опущена на `lg:mt-…`: две колонки, начинающиеся с одной
   высоты, читаются как ячейки таблицы, а здесь это разворот — тот же
   приём сбитой оси, что в диптихе «Как получается кино» и в услугах.

   `data-focus="in"` — кадр приходит расфокусированным и наводится
   скроллом, как все прочие кадры страницы (`reveal.ts`). `TiltFrame` тут
   намеренно НЕ надет: тилт по курсору — язык карточек услуг, где кадр
   работает товаром, а этот кадр — портрет, и вертеть лицо под мышью не то
   же самое, что вертеть карточку услуги.

   п.3 (вторая половина) ССЫЛКА «ОБО МНЕ» ПОД ТЕКСТОМ СНЯТА: «ссылку внизу
   под текстом с надписью "обо мне" убери, думаю хватит той ссылки, что в
   навигации». Зона `copy.about.link` сохранена дормантной. */
function AboutTeaser() {
  /* Ф39 п.2 — звёзды расступаются вокруг фотографии, а не лежат под ней.
     Ссылка отдаётся в `Stars`, который меряет узел сам (см. «ЗОНА
     ИСКЛЮЧЕНИЯ» в `Stars.tsx`). Ref именно на `<figure>`, а не на колонку:
     колонка выше фотографии на высоту заголовка, и зона накрыла бы ещё и
     «Кто снимает» — под заголовком звёздам ничто не мешает. */
  const photoRef = useRef<HTMLElement>(null);

  return (
    /* Ф40: «текст в разделе обо мне должен помещаться в один экран».
       Раздел был 784 px при окне 720 — не влезал на 64 px. Собрано из
       четырёх мест, ни одно из которых не трогает сам текст:
        · поля секции 9vh → 5vh сверху и снизу (−58);
        · сдвиг правой колонки вниз 7vh → 3.5vh (−25) — ось всё ещё сбита,
          просто мягче;
        · правая колонка расширена с 6 колонок до 7 (стартует с той же
          шестой, доходит до края) — это и даёт двум мерам ниже место
          вырасти, без него они упирались в 539 px колонки;
        · меры строк шире: лид 26 → 30 знаков (−1 строка), абзацы 40 → 54
          (−93). Сорок знаков было ниже комфортного диапазона чтения
          (45–75), так что строка стала не только короче по числу, но и
          лучше по мере. */
    <section id="o-mne" className={`${SECT} scroll-mt-[5rem] py-[clamp(2rem,5vh,4rem)]`}>
      <Stars count={7} seed={71} scale="mixed" tones={[1, 2]} avoid={photoRef} />
      <div className={WRAP}>
        <div className="grid grid-cols-1 gap-x-[var(--gap)] gap-y-[clamp(1.6rem,4vw,2.4rem)] lg:grid-cols-12">
          {/* Ф40 (последнее сообщение): «увеличь фотографию немного» —
              колонка 4 → 5, и кадр растёт вместе с ней (своей ширины у него
              нет, он занимает колонку целиком): 346 → 442 px на окне 1280.
              Правая колонка съехала с шестой на седьмую, чтобы освободить
              место, — пустая колонка между ними осталась ровно одна, как и
              была. Тексту это ничего не стоило: он в том же сообщении
              сократился на треть. */}
          <div className="lg:col-span-5 lg:col-start-1">
            <h2 className="reveal t-h2">{copy.about.title}</h2>
            <figure
              ref={photoRef}
              className="reveal m-0 mt-[clamp(1.3rem,3.2vw,2.2rem)]"
              style={{ ['--i' as string]: 1 }}
            >
              <div data-focus="in" className="overflow-hidden">
                <img
                  src={copy.about.photo}
                  width={1200}
                  height={800}
                  alt={copy.about.alt}
                  className="frame block w-full"
                  loading="lazy"
                  decoding="async"
                />
              </div>
            </figure>
          </div>
          {/* ═══ ТЕКСТ: ЛИД КРУПНО, ТРИ АБЗАЦА МЕЛКО ═══
              Ф39 (третье сообщение): «раздел выглядит несуразно из-за
              большого текста… чтобы выглядело стильно и лаконично, было
              понятно и приятно глазу. можно делить текст».

              ЧТО БЫЛО НЕ ТАК. Одна строка на 570 знаков стояла сплошным
              куском одного кегля: тринадцать строк без единой зацепки для
              глаза. Дело было не в объёме — объём её, и он остаётся, — а в
              том, что у текста не было ни начала, ни ступеней. Кегль тут
              лечит мало: крупный даёт стену, мелкий — простыню.

              ЧТО СДЕЛАНО — иерархия вместо однородности, ровно теми же
              средствами, что уже работают на странице:
               · ЛИД (первое предложение, `copy.about.lead`) набран `t-quote`
                 узкой мерой в 26 знаков — это «Я — arisheniaa» и семь лет
                 стажа, то есть ответ на вопрос заголовка. Он один и держит
                 верх блока, как реплики в диптихе «Как получается кино»;
               · ТРИ АБЗАЦА (`copy.about.body`) — `t-body` мерой 40 знаков,
                 по одной мысли на абзац, с воздухом между. Ни один не длиннее
                 четырёх строк, и между ними есть куда выдохнуть;
               · волосяная линейка отделяет лид от абзацев — тот же приём,
                 что делит услуги и шаги, ничего нового на странице.
              Текст не сокращён и не переписан: деление идёт по её точкам,
              склейка частей пробелами даёт исходную строку знак в знак. */}
          <div className="lg:col-span-6 lg:col-start-7 lg:mt-[clamp(1.5rem,3.5vh,3rem)]">
            <p className="reveal t-quote max-w-[30ch]" style={{ ['--i' as string]: 2 }}>
              {copy.about.lead}
            </p>

            <div className="rule mt-[clamp(1.1rem,2.6vh,1.7rem)] max-w-[54ch]" />

            <div className="mt-[clamp(1.1rem,2.6vh,1.7rem)] max-w-[54ch]">
              {copy.about.body.map((para, i) => (
                <p
                  key={i}
                  className={`reveal t-body text-[color:var(--ink-soft)] ${
                    i > 0 ? 'mt-[clamp(0.7rem,1.8vh,1.1rem)]' : ''
                  }`}
                  style={{ ['--i' as string]: 3 + i }}
                >
                  {para}
                </p>
              ))}
            </div>
          </div>
        </div>
      </div>
    </section>
  );
}

/* ═════════════════════════ ФИНАЛЬНЫЙ ПРИЗЫВ ═════════════════════════
   Ф28 сводит эту зону к одной фразе:

     «убери фразу „пишите, даже если ещё не решили“ и ссылку „написать в
      телеграм“. фраза „вы всегда можете связаться со мной, кликнув сюда“
      должна быть единственной, ниже уже контакты. слово „сюда“ сделать
      кликабельной ссылкой с переходом на диалог в телеграме»

   Убрано: заголовок `home:cta.title`, кнопка `home:cta.button`, строка
   `home:cta.channels` («Telegram, VK, телефон» — VK и телефон не подтверждены,
   и Ф28 запрещает добавлять в блок контактов что-либо, кроме двух слов).

   Долг C-09 закрыт: адрес диалога наконец известен — `t.me/arisheniaa`. Раньше
   ссылка вела в эту же зону, потому что адреса не было, и это было записано
   долгом. Больше не долг.

   У секции нет заголовка — впервые на странице. Это прямое следствие правки, а
   не небрежность: единственный заголовок здесь был снятой фразой. Секция
   называет себя навигации и скринридеру через `aria-label`; ориентир для
   пункта «Контакты» из шапки не потерян.

   Кегль фразы поднят с `t-lead` до `t-h2`: она осталась одна на весь экран, и
   на прежнем кегле лида её просто не видно — воздух вокруг стал больше, а
   объект в нём не изменился. Сама строка не тронута ни на знак. */
function Cta() {
  return (
    <section
      id="kontakt"
      aria-label="Контакты"
      className={`${SECT} scroll-mt-[5rem] pt-[clamp(4rem,12vh,9rem)] pb-[clamp(5rem,15vh,11rem)]`}
    >
      {/* Ф39 п.8, самый низ страницы — здесь жёлтого больше всего: 13 → 24
          звезды, три жёлтых на одну бордовую. Чёрный тон (`0`) из этой
          секции убран совсем: под ним последняя фраза и два контакта на
          пустоте, и чёрная россыпь рядом с текстом читается как сор, а не
          как свет. */}
      <Stars count={24} seed={97} scale="mixed" tones={[1, 1, 1, 2]} />
      <div className={WRAP}>
        <div className="mx-auto max-w-[44rem] text-center">
          <p className="reveal t-h2 mx-auto max-w-[24ch] text-balance">
            {copy.cta.textBefore}
            <a
              className="link-major"
              href={copy.cta.href}
              target="_blank"
              rel="noopener noreferrer"
            >
              {copy.cta.textLink}
            </a>
            {copy.cta.textAfter}
          </p>

          {/* КОНТАКТЫ (Ф28). Ровно два кликабельных слова и ничего больше:
              «сделай кликабельными слова „телеграм“ и „инстаграм“… больше
              ничего добавлять не нужно». Ни подписи над блоком, ни адресов,
              ни иконок — иконка соцсети была бы тем самым «ещё чем-то».
              Список, а не строка через точку: два равных адреса — это
              перечисление, и разметка обязана это сказать. */}
          <ul className="reveal m-0 mt-[clamp(2.2rem,7vh,4rem)] flex list-none flex-wrap items-baseline justify-center gap-x-[clamp(1.6rem,5vw,3.4rem)] gap-y-4 p-0"
            style={{ ['--i' as string]: 1 }}
          >
            {copy.contacts.map((k) => (
              <li key={k.word}>
                <a
                  className="link-major link-tap t-h3"
                  href={k.href}
                  target="_blank"
                  rel="noopener noreferrer"
                >
                  {k.word}
                </a>
              </li>
            ))}
          </ul>
        </div>
      </div>
    </section>
  );
}

/* ═════════════════════════ ПОДВАЛ ═════════════════════════
   Ф28 забирает из подвала всё, что в нём было:

     «убери внизу навигацию на фотографии и подпись про самозанятость.
      это лишнее»

   Убрано:
    · дублирующая навигация по фотографиям (`copy.footer.clusters` — шесть
      ссылок на серии и услуги). Она вообще не имела факта за собой;
    · упоминание юрстатуса (`copy.footer.legal`). Ф28 называет это ДЕФЕКТОМ
      СБОРКИ, а не решением владелицы: решение Ф17 — «юрстатус не добавлять на
      сайт сейчас», и билдер поставил заглушку «самозанятая / ИП» сам.
      На сайт не возвращается ни в каком виде;
    · заглушка контактов ⟦Telegram @ник · VK · телефон⟧: адреса теперь есть и
      стоят в блоке контактов, а VK и телефон Ф28 прямо запрещает добавлять;
    · навигация из восьми подписей — переехала в шапку в составе Ф28.

   Осталась волосяная линейка: страница должна кончаться краем, а не обрывом
   последней секции. Экранного текста в подвале больше нет — и это находка для
   гейта, а не готовое решение: если владелица захочет видеть внизу что-то ещё
   (год, права на кадры, политику данных), это новый текст, и его пишет
   копирайтер, а не я. Записано в хендоф.

   `<footer>` сохранён как лендмарк: самопроверка требует ровно один, и
   выкидывать лендмарк ради пустоты нельзя — линейка внизу это тоже подвал. */
export function Footer() {
  return (
    <footer className={`${SECT} pb-[clamp(2rem,5vh,3.2rem)] pt-[clamp(1.5rem,4vh,2.6rem)]`}>
      <div className={WRAP}>
        <div className="rule" />
      </div>
    </footer>
  );
}
