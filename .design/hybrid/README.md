# Гибрид B+C — главная

Сборка по `.design/DIRECTION-LOCK.md` (гейт 3, `FACTS.md` Ф19–Ф27).
Боевой текст — `COPY.md` **revision 6**, прочитан на момент сборки.
Вкусовой скилл — `apple-design`, один, как требует лок.

Живая страница: **http://127.0.0.1:5176/** (dev, порт 5176, `npm run dev`).

**Редакция 2 (Ф28) применена** — правки после первого живого показа владелице.
Розовая тема, переключатель и морфинг имени удалены из кода целиком (не флагом,
не веткой — файлы `theme.tsx` и `NameMorph.tsx` физически удалены). Раздел 7
ниже — точный список изменений с доказательствами; разделы 1–6 описывают
редакцию 1 и помечены инлайн там, где редакция 2 их отменяет.

**Редакция 3 (Ф29, «След + Кино без звука») применена** — семь пунктов
владелицы поверх редакции 2, синтез `RESEARCH-COMPETITORS.md` («фиксируем,
делай!»). **Это не лок**: DIRECTION-LOCK.md не упоминает Ф29 ни строкой,
источник требований — `FACTS.md`. Раздел 8 ниже — точный список с
доказательствами; звук (единственный явно отклонённый пункт черновых пакетов)
не реализован нигде, включая опциональный тоггл.

---

## 1. Что взято из B, что из C

Лок: база — направление B, из C переносятся **три именованных элемента**, не эстетика.

### Из B (база)

| Что | Где в коде | Как перенесено |
|---|---|---|
| Физика рэка | `src/useRack.ts` | Числа не тронуты: слежение 1:1 с сохранением точки захвата, вертикаль 0.35 с резиновым сопротивлением (c = 0.55), проекция импульса `decelerationRate 0.996`, порог переворота 34 % ширины, возврат домой damping 1.00 / response 0.40 с, бросок 0.80 / 0.38 с. Лок: «параметры — как в `directions/b/src/useRack.ts`». |
| Кадр как объект (Р4) | `.frame` в `styles.css` | Ни рамки, ни скругления, ни тени вокруг фотографии. |
| Зерно `feTurbulence` | `Gradient.tsx` → `.grain-veil` | Приём B, но **одним фиксированным слоем на всю страницу**, а не по секциям: под всем лежит непрерывное полотно, и зерно, нарезанное по секциям, дало бы швы на стыках. |
| Вход сцены «наводка на резкость» | `src/reveal.ts` | IntersectionObserver + CSS-переход, плюс скраб расфокуса по прокрутке. |
| Курсорная реакция звёзд | `Stars.tsx`, `REACH = 190` | Отзывчивость сохранена дословно (лок: «меняется только силуэт и палитра звёзд, не их отзывчивость»). |

### Из C — ровно три элемента

1. **Звёзды как фоновая структура** — принцип рассыпки и позиция по композиции, не курсорная механика C дословно. `Stars.tsx`.
2. **Написание никнейма** — гарнитура (**Vollkorn 900**), кегль, позиция в шапке, жёлтая звезда-компаньон слева. `.t-name`, `StarMark`, `Header()`.
3. ~~**Механика перехода имени** — послойное растворение Ф15. `NameMorph.tsx`.~~
   **ОТМЕНЕНО Ф28** (редакция 2): имя статично, файл удалён, см. раздел 7. Из C
   остались только гарнитура/кегль/позиция и звезда-компаньон, без перехода.

### Из C НЕ взято (лок, «Что не входит»)

Тёмная палитра, пиннинг-сцена, GSAP, ScrollTrigger, Lenis. В `package.json` их нет —
проверяется поиском, не обещанием. Прогресс прокрутки считает один `requestAnimationFrame`
на 20 строк (`Gradient.tsx`), вход сцены — IntersectionObserver.
Направление A не участвует.

---

## 2. Как это сделано в коде

### Звёзды — `star-paths.ts` + `Stars.tsx`

Лок: «форма ЗАМЕНЕНА, эталон — файлы владелицы, не код направлений».

- **Пять фиксированных SVG-путей**, снятых с 9 файлов в `для сайта/`, в системе
  координат 100×100: `spark` (звезда5/6), `needle8` (звезда2, звезды.jpg),
  `starfish6` (звезда4), `hands5` (звезды держатся за руки — ключ формы в **вогнутой**
  стороне, квадратичная кривая с контрольной точкой, утянутой к центру на 88 %),
  `tiny8` (звезды на полу).
- **Почему пути, а не canvas:** A/B/C рисовали лучи случайными числами при каждой
  загрузке — силуэт был невоспроизводим и уезжал в компас. Здесь вариативность только
  в масштабе, повороте и наборе; силуэт один и тот же при каждой загрузке, поэтому его
  можно снять и сверить (`shots/star-proof.png`).
- **Три цвета пути** — `STAR_TONES` = `--star-ink` / `--star-gold` / `--star-wine`,
  значения в теме: чёрный `#131110`, жёлтый `#F4B93F`, бордовый `#7A2530` (розовая
  тема с вариантом `#730506` удалена целиком Ф28, см. раздел 7). Непрозрачность
  разная по тону, `TONE_ALPHA`:
  чёрный 0.18–0.40, жёлтый 0.60–0.95, бордовый 0.34–0.62 — подобрано по фону, а не по
  вкусу: чёрный на кремовом кричит, жёлтый на кремовом пропадает.
  Кадры: `shots/stars-0-chern.png`, `stars-1-zhelt.png`, `stars-2-bordo.png`.
- **Расстановка — нерегулярная сетка, а не чистый rand.** Чистый rand сбивается в комки
  и оставляет дыры, и на 390 px комок садится ровно на заголовок. Секция делится на
  ячейки, в каждой не больше одной звезды, точка внутри ячейки смещена
  детерминированным шумом (LCG по `seed`). Ровно так лежат звёзды на плитке в
  `звезды на полу.jpg`: сетка чувствуется, регулярности нет.
- **Физика** — apple-design, числами: пружина критически задемпфирована
  (damping 1.0, response 0.42 с); после импульса от пальца damping 0.72 — овершут
  разрешён только там, где жесту предшествовал импульс; X и Y — независимые пружины;
  цель доворота всегда 0.
- Прокрутка даёт звёздам отставание и пружинный догон — это «реакция на палец» там,
  где пальца на экране нет.

### Градиент — `Gradient.tsx` + `.canvas` / `.mesh`

- **CSS-меш из радиальных слоёв**, не картинка (П10) и не линейная растяжка: у файлов
  владелицы органическое пятновое смешение, линейный градиент даёт полосу.
- Два слоя. `.mesh-base` — три радиальных пятна, двигаются по прогрессу.
  `.mesh-bloom` — про **состав**: `opacity: calc(0.16 + var(--sp) * 0.84)`, на первом
  экране почти выключен, к подвалу набирает полную силу. Поэтому «изменяется при
  прокручивании» не только положением, но и цветом.
- `position: fixed`, весь вьюпорт, `z-index: 0` под контентом — дословно по локу.
- **Прогресс** — одна переменная `--sp` (0…1), которую двигает один rAF. Значение
  округляется до 0.005: при более мелком шаге браузер перекрашивает полноэкранный слой
  на каждый пиксель прокрутки без единого видимого изменения.
- Цвета сняты пипеткой с файлов владелицы, все значения и файл-источник выписаны
  комментарием прямо над каждым набором токенов в `styles.css`.

### Папки отдачи — `Folders.tsx`

- Родительская папка — `<button aria-expanded aria-controls>`, не `div` с `onClick`:
  её надо доставать клавиатурой и она должна сообщать состояние.
- Дочерние **всегда в разметке**, скрыты состоянием, а не `display: none`: контент не
  должен появляться из ничего для поиска и скринридера.
- Раскрытие — `opacity` + `translateX(-38%) scale(0.72)` → `none`, задержка
  `calc(var(--n) * 70ms)`, длительность `--dur-fold: 420ms`.
- Цвет — `--folder-face` / `--folder-back`, переменные темы (архитектура пережила
  Ф28: папка красится темой, а не хардкодом). Сейчас единственная тема — зелёная,
  оливковая. Кадры `shots/folders-01-closed.png`, `folders-02-open.png`.

### Фотографии

- **Ни одного сгенерированного изображения (П10).** Реальные кадры из `Рабочий
  стол\портфолио` и (с редакции 5) `Рабочий стол\исходники`, конвертированы
  `scripts/export-photos.mjs` (sharp → webp). Отбор сделан глазами по контактному листу
  `shots/contact-01…03.png`.
- **12 кадров в рэке** (было 8 до редакции 5, Ф36 п.3 + Ф38 — см. § 10), намеренно
  **не из одной серии**: зима и вода, цвет и чёрно-белое, ретро-телефон и усадьба
  осенью, торт и двойная экспозиция, студийная спина и пара у золочёного алтаря.
  `RACK_FRAMES` в `Rack.tsx`.
- **6 кадров в сетке** «Шесть кадров» — отдельный пул `grid-*.webp` (секция выключена).
- **4 карточки услуг** — три из Ф26, четвёртая («Плёночная») добавлена § 10.
- `loading="lazy"` на всём, кроме первых трёх слоёв рэка; `width`/`height` заданы у
  каждого `<img>`, поэтому вёрстка не прыгает при догрузке.

### Шрифт

| Роль | Гарнитура | Где |
|---|---|---|
| Тело сайта | **Golos Text Variable** (ParaType, OFL, кириллица-first) | всё, кроме имени |
| Служебный слой, цифры | **JetBrains Mono Variable** (OFL) | `.t-mono` |
| **Имя в шапке — единственная антиква** | **Vollkorn 900** (OFL) | `.t-name` |

Лок, «Типографика»: «общий текст сайта — без засечек» отменяет антикву как несущий приём
«везде, **кроме имени в шапке**, которое остаётся антиквой из направления C по отдельному
прямому одобрению». Исключение названо в локе явно, поэтому `.t-name` набран
`--font-display: 'Vollkorn', Georgia, serif` — это та самая гарнитура, которую владелица
одобрила в C (`directions/c/README.md`, «Гарнитуры»: Vollkorn 900).

Взят подрезанный латинский `woff2` из C по `unicode-range` — имя единственный текст
этой гарнитурой на сайте, и после Ф28 оно навсегда латиница, кириллический подрез
для морфинга (был нужен на переход) не грузится вовсе и может быть удалён из
`public/fonts` при следующей уборке — не удалён сейчас, чтобы не трогать лишний
файл без явного повода. Кегль поднят до `clamp(1.25rem, …, 1.75rem)` против 1.0625rem
у гротеска:
брусковая антиква в 900 при том же кегле читается мельче гротеска в 500, и на 1920 имя
терялось. Трекинг выставлен в 0 — минус смыкал засечки в ленту.

**Кириллица проверена на реальном русском тексте, не на панграмме.**

- `shots/font-proof.png` — пробник гротесков на строках из `COPY.md` ред. 6:
  H1, гео, лид, цена. Не латинская панграмма — ровно тот текст, который встал в макет.
- Vollkorn проверен на **самом имени**, то есть на живой странице:
  `shots/name-01-latin.png` (`arisheniaa`), `name-02-during.png` (середина перехода,
  часть букв уже кириллица), `name-03-cyrillic.png` (`аришения`).
- Замером, а не на глаз: оба подрезанных файла отдаются со статусом 200, оба
  `FontFace` в состоянии `loaded`, `document.fonts.check('900 24px Vollkorn', 'аришения')`
  → `true`, и ширина «аришения» в 900/100px = **540 px** против Georgia 560 и
  Golos 513 — метрика отличается от обоих фолбэков, значит глифы рисует сам Vollkorn,
  а не подмена.

---

## 3. Что запрещаю и чем заменяю (правило П3)

Запрет без замены невалиден, поэтому вторая колонка обязательна.

| Запрещаю | Заменяю на | Почему |
|---|---|---|
| `папка.jpg` / `папка2.jpg` как `<img>` (буква Ф24) | тот же силуэт папки **путём SVG**, цвета сняты пипеткой с этих файлов и живут в теме как `--folder-face` / `--folder-back` | оба файла — JPG с белым фоном 500×500. Поверх сплошного градиента белый квадрат виден сразу, а вырезание фона из JPG даёт кайму. И главное: лок требует, чтобы цвет папок зависел от активной темы — растровая картинка тему не слышит, путь SVG красится переменной. Файлы остались источником формы и цвета; выброшен только способ доставки пикселей |
| canvas со случайной генерацией лучей при каждой загрузке (A/B/C) | 5 фиксированных SVG-путей с файлов владелицы, вариативность в масштабе и повороте | воспроизводимость: критик, владелица и скриншот обязаны смотреть одну и ту же страницу |
| GSAP, ScrollTrigger, Lenis | IntersectionObserver + один rAF на прогресс прокрутки | лок: не переносятся. Здесь нет ни пиннинга, ни таймлайна, ни скраба по нескольким целям — библиотека решала бы задачу, которой нет |
| Антиква в теле сайта | Golos Text; антиква только в `.t-name` | лок, «Типографика» |
| Курсив как несущий приём | смена веса и цвета внутри строки | таблица запретов лока |
| Прайс таблицей | цветовая плашка `.plate` (`--plate` / `--plate-ink`), цифра, состав рядом | `PRICE-POSITION.md`: таблица ставит автора в один ряд с теми, кто стоит 3 000 ₽ |
| Плашка цены с изображением-заглушкой | плашка краски без скруглений и тени; фото — только там, где оно настоящее (три кадра Ф26) | таблица запретов лока |
| FAQ на раскрывашках | четыре открытых ответа, вопрос слева крупно, ответ справа узкой мерой | ответ, который надо открыть, на сайте с ценой выше рынка читается как то, что прячут |
| Полноэкранный слайдер, фул-блид как конструкция | кадр-объект с краем на пустоте | таблица запретов лока |
| Сетка одинаковых карточек как способ собрать страницу | **пять разных композиционных схем**: рэк — предмет в руке; «Как получается кино» — диптих со сбитой осью; «Шесть кадров» — разнобой по ширине и вертикали; услуги — три разновеликих разворота; «Как это устроено» — лестница с волосяными линейками | это и есть механизм уплощения: карточка, сетка, карточка |
| Движение в первом кадре (П7) | `--sp: 0` на загрузке (полотно стоит); `is-instant` снимает переход со всего, что видно при загрузке; звёзды стоят на местах и отвечают только на действие; имя — 10 000 мс; скраб расфокуса исключает элементы первого экрана целиком | таблица запретов лока |
| Сгенерированные изображения (П10) | SVG для звёзд и папок, CSS-меш для градиента, `feTurbulence` для зерна, реальные кадры из портфолио для фотографий | таблица запретов лока |
| `innerText` как проверка дословности текста | `textContent` в `scripts/selfcheck.mjs` | `innerText` возвращает текст **после** `text-transform`, и на капительном `.t-mono` давал ложный провал по трём строкам COPY, которые в разметке стоят дословно |

---

## 4. Где боевой текст не встал так, как задумано

Это находки для показа владелице. **Ни одна строка `COPY.md` не переписана и не
сокращена под макет** — где текст не встаёт, это записано здесь, а не подрезано.

1. ~~**BC-03 — «Как получается кино», 21 слово вместо бюджета 14.**~~ **Снято
   редакцией 5 (Ф36 п.5, § 10).** Владелица дала новый текст обеих половин дословно
   сама — прежний текст (ушанка деда, кинжал дяди, газета, кофейня, рассвет), к
   которому относился этот бюджетный конфликт, больше не стоит на странице целиком,
   конфликт бюджета IA.md снят вместе с ним. Композиция (`.t-quote`, диптих со сбитой
   осью) не менялась и держит новый текст так же.
2. **BC-04 — ответ FAQ на 72 слова** (бюджет 40–45). Самый длинный текст на странице.
   Встал мерой 56ch справа от вопроса. Держит, но это предел плотности зоны: если
   владелица добавит хоть одну услугу, ответ придётся уводить на `/price`.
3. **Подписей у кадров рэка нет — и это конфликт, а не забывчивость (число выросло
   с восьми до двенадцати, редакция 5, не меняет саму находку).** `COPY.md` § 1.3
   описывает две половины блока как то, что «держит **две** карточки рэка», то есть
   текст рассчитан на стопку из двух. Лок (Ф19) требует пул из 6–8, редакция 5 (Ф36
   п.3 + Ф38, § 10) расширила его до 12 по прямой просьбе владелицы — лок этим не
   нарушен (просьба владелицы поверх лока — обычный путь редакции, не тихая правка
   лока). Двенадцати подписей в COPY не существует (не было и восьми), а придумывать
   экранный текст билдеру запрещено. Пока: кадры без подписей, положение в стопке
   показывает счётчик `01 / 12`. Нужно либо двенадцать подписей от копирайтера, либо
   решение, что рэк подписей не несёт.
4. **`alt` шести кадров сетки не совпадают с текстом COPY — намеренно.** В COPY они
   стоят как `⟦заглушка⟧` и описывают кадры, которых в выставленных файлах нет: например
   `frame.01.alt` обещает «рука держит мандарин над снегом», а в `grid-mandarin.webp`
   волосы подняты в воздух на песочном фоне. Заглушку нельзя верстать как готовый текст
   (§ 13 п. 11), а `alt` обязан описывать **фактические пиксели**, иначе он врёт
   скринридеру. Написаны по файлам, предметным словарём, без «мягкого / нежного света»
   (§ 0.14), и ни один не совпадает с подписью (§ 11.4). **Отбор кадров руками
   владелицы не сделан** — при замене файлов `alt` переписываются вместе с ними.
5. **Подписи `caption` могут не совпадать с кадрами по той же причине.** «Мандарин над
   снегом» стоит над файлом, где мандарина не видно. Подпись из COPY трогать нельзя,
   файл выбран координатором из 408 (`PHOTO-VERDICT.md`) — расхождение снимается
   отбором владелицы, не правкой текста.
6. **Служебный слой набран капителью, а COPY даёт эти строки строчными.**
   `.t-mono` несёт `text-transform: uppercase`, поэтому «Снимаю в Москве и Туле»,
   «Придумано заранее», «Найдено на месте» и подписи кадров читаются с экрана
   капителью. В разметке они стоят дословно (проверено `textContent`), но визуально это
   решение поверх текста, и владелица должна его увидеть и подтвердить или снять.
   Две интерактивные подписи — «нажми» и «нажми на меня» — капители **не получили**:
   § 0.18 требует строчных, и они строчными и стоят.
7. **Фото Ф26 не совпадает с описанием Ф26.** Для «одного человека» Ф26 обещал
   «зелёный бархат, тёплый естественный свет», в файле `IMG_5439.JPG` — чёрно-белый
   кадр у оливы в тумане. Кадр оставлен (он назван в локе по имени файла), `alt`
   описывает пиксели. Замена — при показе.
8. **Блок «Что говорят» не открыт.** OQ-05: «отзывы прикреплю позже». Зон девять, все
   пустые, заполнять запрещено — блока на странице нет вовсе.
9. **Подвал: 7 ссылок из 8 ведут внутрь этой страницы.** Собран один URL из десяти
   (волна 1), карта временных адресов — `NAV_STUB` в `App.tsx`. Заглушки `contacts` и
   `legal` показаны **в угловых скобках и приглушённо** (C-09 / C-10), чтобы их нельзя
   было принять за рабочие адреса. Это заявленный долг, не решение.
10. **«кликнув сюда» ведёт в зону контакта на этой же странице.** Ф27 требует рабочую
    ссылку, адрес Telegram не подтверждён (C-09). Как только адрес есть — одна строка
    в `copy.ts`.
11. ~~**OQ-18 — переключатель тем видимый.**~~ **Закрыто Ф28.** Владелица увидела обе
    темы на живом показе и выбрала зелёную окончательно («тему оставь в зелёном
    стиле… очень нравится, особенно общее настроение сайта»). Розовая тема и
    `ThemeSwitch` удалены из кода, не спрятаны флагом — см. раздел 7.
12. **В `prefers-reduced-motion` первый экран выше вьюпорта.** Рэк разворачивается в
    сетку (было восемь кадров, редакция 5 расширила до двенадцати — `RACK_FRAMES`,
    ничего не потеряно, `shots/reduced-motion-rack.png`), и она выше, чем стопка. Это
    осознанный обмен: двенадцать кадров в одну колонку превратили бы первый экран в
    экран прокрутки, а сетка 2×6 хотя бы читается целиком — компромисс тот же, что был
    при восьми, разница только в числе строк сетки.
13. **На 390 px счётчик и кнопка рэка уходят под сгиб.** Сам рэк на первом экране виден
    и берётся пальцем, но его подпись `01 / 12` (было `01 / 08`, редакция 5) — ниже.
    Кадр `shots/hero-390-green.png`.

---

## 5. Самопроверка

`node .design/hybrid/scripts/selfcheck.mjs` — печатает измерения, не мнения, и выходит
кодом 1 при любом провале. Проверяет:

- `scrollWidth === clientWidth` на **360 / 375 / 390 / 768 / 1280 / 1920** в обеих
  темах — 12 замеров, равенство, а не «почти»;
- лендмарки по одному (`main`, `header`, `footer`, `h1`) — два лендмарка на кадре
  выглядят ровно как один, ловится только замером;
- иерархию заголовков без дыр;
- дословность 12 строк COPY по `textContent`;
- `prefers-reduced-motion` не теряет контента: сравнение числа заголовков, длины текста
  и числа `<img>` с обычным режимом;
- кириллический дубль имени в `<title>` (П8) — экранное имя теперь всегда латиница,
  морфинг снят Ф28, проверка адаптирована под редакцию 2;
- ошибки консоли и `pageerror` на каждой ширине.

**Проба на красное.** Флаг `--redproof` подставляет заведомо отсутствующую строку и
инвертирует порог скролла: скрипт печатает 24 FAIL и выходит кодом **1**. Боевой прогон —
все проверки `ok`, код **0**. Зелёный отчёт здесь доказан умеющим краснеть, а не принят
на слово.

`./node_modules/.bin/tsc --noEmit` — 0 ошибок; та же проба на красное (подложенное
`const x: number = "нет"`) даёт `TS2322`, то есть компилятор действительно смотрит файлы.

### Две находки самопроверки, которые были настоящими дефектами

1. **`vite.config` отсутствовал целиком.** Vite собирался успешно и печатал зелёный
   отчёт — ругался только lightningcss, предупреждением, кодом 0. При этом
   `@tailwindcss/vite` не подключался, директивы `@tailwind utilities` и `@theme`
   уходили в сборку **необработанными**, и в CSS не было ни одной утилиты: ни `flex`,
   ни `grid-cols-12`, ни `mx-auto`. Страница разваливалась в одну колонку, `--font-sans`
   не существовал, шрифт падал на системный. Проверка теперь не «сборка прошла», а
   «в `dist/*.css` есть правило `.grid` и нет `@tailwind`».
2. **Порт слушался только по IPv6.** `localhost` на этой машине разрешался в `::1`,
   Vite слушал `[::1]:5176`, Playwright шёл по IPv4 и получал `ERR_CONNECTION_REFUSED`
   при живом сервере. Отчёты были зелёными с обеих сторон: Vite печатал «ready»,
   Playwright — «refused». В конфиге теперь явный `host: '127.0.0.1'`.

Обе — ровно тот случай, о котором предупреждает методология: число в отчёте было
счётчиком чего-то другого, а не результатом проверки.

### Что ещё было исправлено при дозавершении

- `H1` отдавал `textContent` = «Создаювашекино»: три `<span class="block">` стояли
  вплотную, без пробельного узла. Визуально лестница выглядела верно, а копирование,
  поиск и скринридер получали одно слово. Между блоками поставлен пробел (П8).
- `.canvas` не обрезал свои слои. `.mesh` намеренно вылезает за край на 18 % (иначе на
  ходу открывался угол сплошного `--m-base`), но без обрезки этот вылет попадал в
  `scrollWidth` и давал горизонтальную прокрутку: 19 px на 390, 43 px на 1280.
  Добавлены `overflow: hidden` на `.canvas` и `overflow-x: clip` на корне (`clip`,
  а не `hidden`: `hidden` на корне делает html скролл-контейнером).
- Стопка рэка выступает вправо-вниз на `PEEK × глубину` = до 39 px и упиралась в край
  колонки. `.rack-stack` получил `margin-right/bottom: 39px` — к краю колонки теперь
  приходит **видимый край стопки**, а не грань верхнего кадра. Оптически это и вернее.
- Три папки в ряд не влезали в колонку 4/12 на 1280 и выдавливали строку за край.
  `.delivery-stage` получил `flex-wrap: wrap` — перенос вместо сжатия: сжатая до 60 px
  папка перестаёт читаться папкой.
- `index.html` нёс мета-описание **предыдущей** редакции («Москва — 7 000 ₽ за час»),
  снятое в ред. 6 как запрещённое (§ 14, «Единая ставка 7 000 ₽ — ЗАПРЕЩЕНА, Ф23»).
  Приведено к `home:meta.description` ред. 6.

---

## 6. Доказательства — `shots/`, 38 кадров (редакция 2, обновлено Ф28)

Пересняты после правок Ф28 — тем и морфинга в кадрах больше нет, набор отражает
только зелёную тему и статичное имя. Прежние `*-pink.png` и `name-02-during`/
`name-03-cyrillic` удалены как доказательства несуществующего состояния.

| Что доказывает | Файлы |
|---|---|
| Первый экран, 4 ширины | `hero-{390,768,1280,1920}.png` |
| Навигация в шапке: закрыто/открыто/проскроллено | `nav-01-1280`, `nav-02-1920`, `nav-03-390-closed`, `nav-04-390-open`, `nav-05-1280-scrolled` |
| Имя статично: t0 / +11с / кроп | `name-01-t0`, `name-02-t11s`, `name-03-crop` (идентичны — доказывают отсутствие изменения) |
| Рэк: покой / тяга / после переворота / 390 px | `rack-01-rest`, `rack-02-drag`, `rack-03-flipped`, `rack-04-390` |
| Папки: закрыто / открыто / подпись крупно / 390 | `folders-01-closed`, `folders-02-open`, `folders-03-label-crop`, `folders-04-390-open` |
| Звёзды крупно, три цвета пути + свечение | `stars-0-chern`, `stars-1-zhelt`, `stars-2-bordo`, `stars-3-glow-pointer` |
| Силуэты всех пяти путей рядом | `star-proof.png` |
| Карточки услуг (4, включая плёнку) | `services-01-1280`, `services-02-390`, `services-03-1920` |
| CTA — одна фраза, без кнопки | `cta-01-1280`, `cta-02-390` |
| Подвал — без навигации по фото и юрстатуса | `footer-01-1280`, `footer-02-390` |
| `prefers-reduced-motion`, ничего не потеряно | `reduced-motion-full-1280`, `reduced-motion-full-390` |
| Вся страница целиком | `page-full-{390,1280,1920}.png` |
| Кириллица на боевом русском тексте | `font-proof.png` |
| Контактный лист отбора кадров | `contact-01…03.png` |

Съёмка — `node .design/hybrid/scripts/shots.mjs` при поднятом сервере на 5176.
Крючки съёмки живут только в адресной строке и в продуктовом пути отсутствуют:
`?rack=-96` (держит верхний кадр вытянутым: у headless нет пальца), `?startone=0|1|2`
(сводит россыпь к одному тону для съёмки цвета крупно), `?pointer=x,y` (виртуальный
курсор для звёзд). `?morph=slow` и `?theme=pink` — крючки редакции 1, мертвы после
Ф28 (нечего растягивать и нечего переключать), убраны из этого списка.

**Кадры считаются черновыми.** Это dev-сборка для критика. Финальный evidence-бандл
снимает `dp-auditor` с прод-сборки одним координированным проходом.

---

## 7. Редакция 2 — правки после первого живого показа (Ф28)

Дата: 2026-08-02/03. Владелица посмотрела редакцию 1 (разделы 1–6 выше) вживую и
дала девять точечных правок, дословно записанных в `FACTS.md` Ф28 и
`DIRECTION-LOCK.md`. Источник истины — `FACTS.md`, здесь — что из этого сделано в
коде и чем доказано. Changelog в коде — `App.tsx`, комментарий «РЕДАКЦИЯ 2».

| № | Правка (дословно от владелицы) | Реализация | Доказательство |
|---|---|---|---|
| 1 | «пусть [имя] не будет кликабельным, и не будет меняться. оставь его на латинице» | `NameMorph.tsx` удалён, `Name.tsx` — статичный `<p>`, не ссылка, не кнопка. Гарнитура/звезда из C сохранены | selfcheck: «имя не изменилось за 11 с», «имя не кликабельно — не ссылка и не кнопка» |
| 2 | «тему оставь в зелёном стиле… очень нравится» | `theme.tsx`, `ThemeSwitch`, `[data-theme=pink]`, `.swatch` удалены из кода физически, не флагом | `grep -i pink src/` пуст (кроме имени файла-референса цвета в комментарии `styles.css`) |
| 3 | «звёзды должны светиться, как в варианте C» | `filter: drop-shadow(...)` на `Stars.tsx`, форма/цвета Ф24 не тронуты | selfcheck: «звёзды светятся: filter = drop-shadow(rgb(244, 185, 63) 0px 0px 7.5px) drop-shadow(...) 0px 0px 2.5px)»; `shots/stars-3-glow-pointer.png` |
| 4 | «навигацию сделай в верхушке сайта… главная / работы / обо мне / что снимаю / подготовка к съёмке / контакты» | Шесть пунктов дословно в шапке, `@arisheniaa` — личный контакт (не путать с каналом `@byarisheniaa`) | selfcheck: дословность всех шести подписей; `shots/nav-01…05.png` |
| 5 | «убери внизу навигацию на фотографии и подпись про самозанятость» | Оба убраны из `Footer` | selfcheck: «снято Ф28: „самозанят“», «„ИП,“», «„Серия «»“» — все отсутствуют |
| 6 | «фраза „вы всегда можете связаться со мной, кликнув сюда“ должна быть единственной… „сюда“ — ссылка на телеграм» | `home:cta.title` и кнопка убраны, `home:cta.text` — единственный текст, «сюда» → `t.me/arisheniaa` | selfcheck: «в блоке CTA нет кнопки „Написать в Telegram“», «слово „сюда“ — ссылка… получено https://t.me/arisheniaa» |
| 7 | «текст „нажми на меня“ сделай на самой папке» + новый текст пояснения | Подпись перенесена на папку, `home:delivery-demo.note` заменён дословно | selfcheck: дословность «нажми на меня», «Пришлю ссылочку на папку…» |
| 8 | «возьми IMG_5511 для индивидуальной… IMG_6281 для творческой… IMG_1651 для плёнки. и уменьши их в размере» | Три фото заменены + новый слот «плёнка», показ уменьшен до потолка 304 px | selfcheck: «кадров услуг четыре, включая плёнку», «показ… самый крупный 304 px при потолке 304 px», «слот «плёнка» на месте» |
| 9 | «6 фотографий, которые ты выбрал, я бы убрала пока. если что, добавим позже» | `<Grid />` закомментирован в `App.tsx`, зоны `home:grid.*` и компонент **не удалены** — возврат: одна строка | selfcheck: «снято Ф28: „Шесть кадров“ — секция home:grid — выключена» |

### Дважды подтверждённые находки при редакции 2

1. **`vite.config` отсутствовал, `@tailwindcss/vite` не подключался** — сборка была
   зелёной, но без единой Tailwind-утилиты в CSS. Найдено самопроверкой, не глазом.
2. **Порт слушался только по IPv6** (`[::1]:5176`), Playwright по IPv4 получал
   `ERR_CONNECTION_REFUSED` при живом сервере — оба отчёта были зелёными с разных
   концов. Исправлено явным `host: '127.0.0.1'` в `vite.config.mts`.

Оба случая — то же предупреждение методологии, что и в редакции 1 (раздел 5, «две
находки»): зелёный отчёт может считать не то, что должен.

### Текущий статус (проверено заново при возобновлении работы)

- `node scripts/selfcheck.mjs` — **зелёный, все проверки пройдены**, включая полный
  список редакции 2 выше.
- `./node_modules/.bin/tsc --noEmit` — **0 ошибок**.
- Живой сервер на 5176 отдаёт страницу без ошибок консоли на всех проверяемых
  ширинах.
- Открытые пункты, которые редакция 2 не закрывала и не обязана была: рэк без
  подписей у восьми кадров (нужны от копирайтера или решение не подписывать),
  `alt`/подписи сетки «Шесть кадров» рассинхронны с реальными файлами (секция
  выключена, но зоны живы на будущее), блок отзывов пуст (OQ-05), Telegram-адрес
  в подвале как отдельный контакт уже есть (`@arisheniaa`), юридический статус на
  сайт не возвращается ни в каком виде (Ф17).
- Следующий шаг по методологии — Ф5, `dp-auditor`: финальный аудит по восьми
  линзам с evidence-бандлом с прод-сборки (`npm run build`), не с dev-сервера.

---

## 8. Редакция 3 — «След + Кино» без звука (Ф29)

Дата: 2026-08-03. **Не лок** — `DIRECTION-LOCK.md` не упоминает Ф29 ни строкой,
источник требований целиком `FACTS.md`. Синтез `RESEARCH-COMPETITORS.md` в два
пакета («След» — материальность ремесла, «Кино» — атмосфера и режиссура скролла),
показанных владелице; она свела их сама: «Собери оптимальный вариант из Следа и
Кино — хочу выразительно, динамично, увлекающе и без звуков», затем подтвердила
список семи пунктов: **«фиксируем, делай!»**. Обе половины двойственности автора
(«тихое»/«странное») по-прежнему держатся поровну — ни один пункт не привязан
к одному регистру.

| № | Правка | Реализация | Доказательство |
|---|---|---|---|
| 1 | Переход настроения синхронно со скроллом между «тихим» и «странным» | Новая переменная `--mood` (`Gradient.tsx`) — ЛОКАЛЬНЫЙ прогресс одной секции (диптих «Как получается кино», `data-mood-scope`), не общий `--sp` страницы: формула `(vh − top) / (vh + height)`, квант 1/200, кроссфейд `.mesh-mood-a`/`.mesh-mood-b`. Реализует дословно принцип `BRIEF.md` «свечение, а не контраст», уже работающий у звёзд, теперь и у смены регистра | selfcheck: «`--mood` у начала диптиха около 0», «у конца около 1», «растёт непрерывно со скроллом (0 → 1)», «у конца „странный“ слой ярче „тихого“» |
| 2 | Курсор-метка «тяни» над рэком | Инлайн `cursor: grab/grabbing` (`Rack.tsx`) убран, системный курсор не переопределён (`cursor: none` нигде нет) — рядом рисуется второй самостоятельный слой: силуэт `tiny8` (тот же, что у фоновой россыпи звёзд) + слово «тяни» строчными, тот же регистр, что у «нажми»/«нажми на меня» (`COPY.md` § 0.18). Слово дано дословно в задании, не сочинено | selfcheck: «над рэком не системный grab/grabbing (computed cursor: auto)», «метка не показана, пока курсор не над рэком», «появляется над верхним кадром», «текст метки — „тяни“», «пропадает при взятии/уводе курсора» |
| 3 | 3D-тилт карточек услуг, 4–6° | Новый компонент `Tilt.tsx` (`TiltFrame`) — наклон на самом КАДРЕ (Р4: «кадр — объект»), не на карточке-обёртке; середина вилки 5°, CSS-переменные `--tilt-x`/`--tilt-y`/блик, отдельный узел от `[data-focus]` (`reveal.ts`) во избежание конфликта двух транформов на одном элементе. Мышь-онли (`pointerType==='mouse'`), под `prefers-reduced-motion` обработчики не вешаются вовсе | selfcheck: «наведение меняет матрицу transform кадра», «блик по краю появляется при наведении», «тилт возвращается в плоское положение при уходе курсора»; `shots/tilt-01-hover.png` |
| 4 | Боковой индикатор разделов маленькими звёздами | Новый компонент `SectionStars.tsx` — путь `tiny8` из `star-paths.ts` (тот же словарь, что фоновая россыпь), список секций читается живьём из `main > section` (не хардкод id — не может разойтись со страницей), активная — `IntersectionObserver` с полосой `rootMargin: -46%/-46%` по центру экрана. Второй `<nav>` на странице — осознанно: `aria-label="Прогресс по разделам"` отличается от шапочного «Разделы сайта», это два различимых лендмарка, не дубль (`selfcheck.mjs` проверяет два РАЗНЫХ `aria-label`, не сам факт количества). Скрыт до `lg` (1024px) | selfcheck: «по звезде на секцию (7 звёзд, 7 секций)», «ровно одна активная звезда в начале», «после прокрутки в конец активна последняя», «клик по первой звезде возвращает к началу» |
| 5 | EXIF-подпись на плёночных кадрах | **Сужено против исходного черновика находкой, не решено молча** — см. OQ-21 (`FACTS.md`): исходники не несут EXIF, марка плёнки не факт, а город конкретного кадра прямо не привязан (`COPY.md` § 0.11). Новый компонент `FilmCaption.tsx` показывает только слово «плёнка» (уже в боевом тексте, не изобретено), и только на кадре, для которого плёнка подтверждена фактом (`services/plenka.webp`, комментарий `export-photos.mjs`) — не на кадрах рэка, где материал не подтверждён. Мышь — по наведению; тач — 480 мс удержания, метка держится ещё 1,8 с после отпускания; клавиатура — `:focus`/`:blur` | selfcheck: «подпись скрыта по умолчанию / появляется при наведении», «показывает только подтверждённый факт „плёнка“», «на странице нет выдуманных технических EXIF-деталей (OQ-21)» |
| 6 | Цена — за кликом внутри плашки | Новый компонент `PricePlate.tsx` — цифра не вырезана из текста (правило «боевой текст дословно»: в COPY нет отдельной версии без цифры), а приглушена `filter: blur(6px)`, тот же приём, что уже используется у капители служебного слоя (README § 4 п. 6). Побочный эффект — доступность бесплатно: `blur` не прячет текст от скринридера, полная цена в DOM и озвучивается сразу, независимо от клика | selfcheck: «цифра приглушена ДО клика: filter=blur(6px)», «проявляется ПОСЛЕ клика: filter=blur(0px)», «aria-expanded переключился», «цена целиком в DOM даже в закрытом состоянии» |
| 7 | Асимметричный разлёт папок отдачи | `Folders.tsx` — раньше обе дочерние папки раскрывались одной формулой с разницей только в 70-мс сдвиге таймера; теперь угол, сдвиг по X, масштаб и длительность перехода заданы вручную по `:nth-child(1)`/`(2)` в `styles.css`, не производной от индекса — два ручных набора чисел, не симметричная функция | selfcheck: «разлёт асимметричен по углу/сдвигу», «разная скорость раскрытия (0.4s / 0.47s)», «разный тайминг (0s / 0.09s)» |

**Явно не реализовано: звук.** Черновые пакеты предлагали звук затвора при
перевороте рэка (`RESEARCH-COMPETITORS.md` § 5); владелица прямо попросила «без
звуков» при сведении пакетов — не перенесено ни в каком виде, включая
опциональный тоггл. `selfcheck.mjs` проверяет это явно: red-proof на синтетическое
звуковое нарушение плюс проверка «нет звуковых API в `src/`» по всем 16 файлам.

### Текущий статус

- `node scripts/selfcheck.mjs` — **зелёный**, все проверки редакций 1–3 пройдены разом.
- `./node_modules/.bin/tsc --noEmit` — **0 ошибок**.
- Новые доказательства в `shots/`: `tilt-01-hover.png`, `price-plate-01-closed.png`,
  `price-plate-02-open.png`, `section-stars-01-top.png`, `section-stars-02-mid.png`,
  `rack-hint-tyani.png`, плюс пересъёмка кадров, задетых изменениями (`rack-*`,
  `services-*`, `nav-*`, `page-full-*`, `reduced-motion-full-*`).
- Открытый пункт — **OQ-21** (марка плёнки владелицы): EXIF-подпись работает в
  честной урезанной версии («плёнка», без выдуманных настроек камеры). Как только
  факт появится — расширяется до «плёнка · <город>» правкой строки, не механики.
- Прочие открытые находки редакции 1–2 (подписи рэка, `alt` сетки «Шесть кадров»,
  пустой блок отзывов OQ-05, юрстатус) — без изменений, см. разделы 4 и 7.

---

## 9. Исправление имени в шапке — Vollkorn убран, найдена ошибка чтения (Ф35)

Дата: 2026-08-04. Владелица попросила сверить шрифт и размер никнейма с направлением
C дословно («точно таким же, как в варианте C»). Сверка вскрыла находку: `.t-name`
стоял на `--font-display` (Vollkorn 900) с комментарием «гарнитура, которую владелица
одобрила… это Vollkorn 900» — но это была ошибка чтения таблицы гарнитур C, не факт.

`directions/c/README.md`, таблица «Гарнитуры»: «Дисплей — Vollkorn 900 (**H1/H2**)» —
Vollkorn относится к крупным заголовкам, не к имени. `directions/c/src/App.tsx`, обе
точки использования имени (шапка и вторая копия в подвале) — `<NameMorph
className="text-[1.0625rem] font-medium tracking-tight">` и `<NameMorph
className="text-[0.9375rem]">`, ни разу без переопределения `font-family`: имя
наследует обычный текстовый Golos Text. Антиквы в имени C не было никогда.

**Исправлено**: `.t-name` — `font-family: var(--font-sans)` (Golos Text), `1.0625rem`,
`font-weight: 500`, `letter-spacing: -0.025em` (точные числа шапки C). Звезда-компаньон
`StarMark` — `size={18}` (было 17, тоже точное число C). Удалены оба `@font-face` для
Vollkorn и оба файла `public/fonts/vollkorn-900-n-*.woff2` — гарнитура больше нигде на
сайте не используется, тянуть её вес незачем.

Ирония находки: в истории проекта это уже ВТОРОЙ разворот на 180° по этому же
элементу (первая версия стояла на Golos, её заменили на Vollkorn, посчитав это
переносом C — теперь выяснилось, что первая версия была верной, а замена ошибочной).
Записано максимально явно в комментариях `Name.tsx`/`styles.css`, чтобы третьего
разворота без новой прямой сверки с исходником не случилось.

Проверено: `tsc --noEmit` — 0 ошибок; `node scripts/selfcheck.mjs` — зелёный (все
проверки редакций 1–3, включая измерение реального `getComputedStyle` имени в браузере:
`font-family: "Golos Text Variable"`, `font-size: 17px`, `font-weight: 500`).

---

## 10. Редакция 5 — навигация, тексты, услуги, цена, «как устроено» (Ф36 + Ф37 п.1–4)

Дата: 2026-08-04/05. Девять пунктов Ф36 («Редакция 5 главной страницы») плюс четыре
правки Ф37, пришедшие следом, пока билдер уже работал над Ф36 («Правит/дополняет
Ф36»). Источник истины — `FACTS.md`, цитаты владелицы дословно. **Не лок** —
`DIRECTION-LOCK.md` эту редакцию не упоминает ни строкой; ни один пункт лока не
поправлен, конфликтов с локом не найдено.

Предыдущий заход на эту же правку упал по исчерпанию лимита трат до первой записи
на диск в `App.tsx`/`copy.ts`/`PricePlate.tsx`/`Folders.tsx` — эта редакция начата
заново с чистого кода поверх уже готового экспорта фотографий
(`scripts/export-photos.mjs`, план и решения по составу рэка и карточек услуг —
координатора, не изменены).

### Что сделано, по пунктам Ф36

1. **Навигация** — «Работы» снята целиком, «Обо мне» переставлена на второе место
   (сразу после «Главная»), «Подготовка к съёмке» → «С чего начать». `copy.ts`
   (зеркальный массив `copy.nav`, нигде не используемый кодом, обновлён для
   консистентности) и `NAV` в `App.tsx` (реальный источник разметки шапки).
2. **Первый экран, второе предложение лида** — единственное место в проекте,
   где владелица прямо просит сочинить текст по примеру, а не дала готовую
   строку. Написано по образцу «создадим ваше кино, запечатлим в статичных
   кадрах» (её опечатка «запечалим» не воспроизведена — явно оговорена в
   FACTS.md как описка): `Снимаю на цифру и на плёнку. Создадим ваше кино —
   запечатлим его в статичных кадрах.` Первое предложение не тронуто.
3. **Рэк — 12 кадров** (было 8). `Rack.tsx`: `doroga`/`krasnoe` убраны (решение
   координатора, задокументировано в `export-photos.mjs`), добавлены
   `telefon, usadba, tort, dvoinaya, spina, altar` со своими `alt` — предметным
   словарём, без «мягкого/нежного» (§ 0.14), написаны билдером по факти­ческим
   пикселям файлов (просмотрены как изображения перед написанием). Порядок в
   массиве — на усмотрение билдера, кроме одного условия Ф38: `altar.webp`
   должна попасть «в первое перелистывание снимков на главной» — поставлена
   вторым кадром стопки (видна после одного взмаха, `01/12` → `02/12`; доказано
   `shots/rack-03-flipped.png` и selfcheck 6.1). Блок-комментарий над
   `RACK_FRAMES` и раздел «Фотографии» этого README обновлены на «двенадцать».
4. **Кнопка «Написать в Telegram» мельче.** Решение билдера (конкретное
   значение не названо владелицей): класс сменён с `t-h3` (step-2, до 1.95rem)
   на `t-lead` (step-1, до 1.4rem) — соседняя ступень уже существующей
   типографской шкалы, не магическое число. Первая ссылка осталась крупнее
   второй («Сначала посмотреть съёмки», `t-body`) — она первична, но разница
   меньше прежней.
5. **Диптих «Как получается кино»** — новый текст обеих половин дословно
   владелицы, строчными, её пунктуация сохранена буквально (включая двойную
   точку «воду..» и три точки в конце). Метки-заголовки «ПРИДУМАНО ЗАРАНЕЕ» /
   «НАЙДЕНО НА МЕСТЕ» сняты. `copy.ts` (`cinema.a/b.text`, поля `.label`
   удалены), `App.tsx` (`Cinema()` — абзацы `.t-mono` с меткой убраны).
6. **Крупные заголовки секций уменьшены.** Находка при проверке: FACTS.md
   называет их класс `.t-h2` — по факту в коде (`App.tsx`) оба названных
   примера (`home:offer.title`, `home:cinema.title`) набраны `.t-h1`, и
   никакой другой заголовок страницы этот класс не использует. «Подобные»
   поэтому ничего не добавляет — множество уже точное. `.t-h1` в `styles.css`:
   `clamp(1.95rem, 1rem + 4.2vw, 4.5rem)` → `clamp(1.7rem, 0.95rem + 3.4vw,
   3.6rem)` (~20% по обеим границам вилки — «чуть меньше», не радикально).
7. **Новая вводная строка блока услуг** — дословно, строчными, без точки:
   `на ваш выбор классика и творчество — к любой из них можно добавить
   пленочные воспоминания` (`copy.offer.lead`).
8. **Карточки услуг** — самый плотный пункт, четыре под-правки:
   - **Верстка плотнее.** «не делай такого большого разрыва между
     фотографиями... я представляю как услуги описаны справа от фотографии».
     `OfferRow` (карточки «Индивидуальная»/«Парная») была вертикальным стеком
     (фото над текстом с зазором `mt-[clamp(1.1rem,2.4vw,1.6rem)]`) — стала
     горизонтальной парой (`sm:flex-row`, фото слева/текст справа, зазор
     `0.7–0.8rem`), кадры уменьшены (15/13 rem → 9/8 rem — крупный кадр в
     боковой раскладке столкнул бы текст за колонку). На 360–390 px раскладка
     остаётся вертикальной (`flex-col` до `sm:`): боковая раскладка при 128 px
     кадра и узкой рабочей ширине сжала бы русский текст до нечитаемого
     столбика — доказано `shots/services-02-390.png`, слова не рвутся.
   - **Фото и подписи** по таблице Ф36: «Один человек»→«Индивидуальная»
     (новое фото, подпись сокращена ровно до «30 снимков за час в авторской
     обработке»), «Двое»→«Парная» (новое фото, подпись без изменений),
     «Творческая» (фото заменено, подпись не тронута — «поменяй подпись...
     DSC_0065» прочитано как «замени фото», текст не назван явно). Файлы
     переименованы вместе с картой: `odin.webp`→`individualnaya.webp`,
     `dvoe.webp`→`parnaya.webp` (уже сделано в `export-photos.mjs`); старые
     `odin.webp`/`dvoe.webp` удалены с диска, `doroga.webp`/`krasnoe.webp`
     тоже (грепом подтверждено — больше нигде не упоминаются).
   - **Формат цены «город — цена/час».** Было «X ₽ за час в Москве · Y ₽ в
     Туле», стало `Москва — 6 000 ₽/час, Тула — 4 000 ₽/час`. Творческая
     (не почасовая) получила тот же принцип с иной единицей: `Москва —
     8 000 ₽ за съёмку, Тула — 6 000 ₽ за съёмку`.
   - **Плашка цены открыта сразу, «дверца снизу вверх».** ОТМЕНА Ф29 п.6:
     раньше цифра была приглушена `blur` и ждала клика, теперь видна сразу —
     владелица прямо: «чтобы было сразу видно стоимость и город». Решение
     билдера по механике (конкретный приём и тайминг не назван): `clip-path`
     вместо `blur`, триггер — существующий примитив `.reveal`/`is-sharp`
     (`reveal.ts`, IntersectionObserver), НЕ новый JS («движение из
     примитивов»). Закрыто — `inset(100% 0 0 0)` (нулевая видимая высота,
     прижата к верху), открыто — `none`; переход `520ms var(--ease-spring)`
     с задержкой `120ms` — числа билдера, подобраны на глаз по ощущению
     «дверцы», не названы владелицей. `.price-plate` стал `<span>`, не
     `<button>` — открывать по клику больше нечего, некликабельная кнопка
     была бы отдельным нарушением доступности. `PricePlate.tsx`, `styles.css`.
   - **Четвёртая карточка «Плёночная».** Прежний безымянный слот «плёнка»
     (Ф28, текст `home:offer.note` рядом) стал полноценной карточкой со своим
     именем и подписью — дословно владелицы, строчными, числа её словом
     «рублей» (не знаком «₽» — её словарь в этой конкретной цитате не
     заменён): `по желанию — сделаем пленочные воспоминания: 3 000 рублей на
     36 кадров и 5 000 рублей за 72 кадра (рекомендуемая длительность съёмки
     с пленкой — от 1,5 часов)`. Старый `offer.note` — зона сохранена
     дормантной (тот же принцип, что `home:grid`, Ф28: не удалять зоны COPY),
     не рендерится — держать оба текста разом означало бы дублировать
     содержание рядом, прямо против «не делай большого разрыва».
   - **Ссылка «Весь прайс и условия» снята целиком.** Зона `copy.offer.cta`
     сохранена дормантной, не рендерится.
9. **«Как это устроено» → «С чего начать и как это всё устроено».** Новый
   заголовок — не вторая строка рядом со старой, а полное имя секции (по
   контексту цитаты и согласованию с переименованием пункта навигации, п.1).
   Тексты шагов 1–2 заменены дословно Ф36, шаги 3–4 (Ф36 говорила «не
   трогать») — заменены следом Ф37 п.1–2, которая прямо это отменяет. Новый
   факт — размер предоплаты **2 000 ₽**, назван впервые (раньше, Ф17/OQ-12,
   был закрыт только способ оплаты). Ярлыки шагов (`label`) нигде не
   тронуты — ни одна цитата не даёт новый ярлык, только текст.

### Что сделано по Ф37 (пункты 1–4, «Правит/дополняет Ф36»)

1. Шаг 3 «День съёмки» — новый текст дословно.
2. Шаг 4 «Отдаю кадры» — новый текст дословно (отменяет прежнее «шаги 3–4 не
   трогать» из Ф36).
3. **Пояснение под папками отдачи переехало НАД них.** «текст, который сейчас
   под папкой, должен быть над папкой» — `Folders.tsx`: параграф `note`
   переставлен с позиции после `.delivery-stage` на позицию до неё (было
   `mt-`, стало `mb-`, тот же зазор). Сам текст не тронут. Доказано
   `shots/folders-01-closed.png` и selfcheck 6.4 (порядок в DOM).
4. **Раздел «Помогу подготовиться к съёмке» убран целиком.** `<Faq />` не
   рендерится в `Page()` — тот же принцип, что `<Grid />` (Ф28): компонент и
   зоны `copy.faq` сохранены, не удалены, возврат — одна строка.

### Находка координатора при выключении FAQ — конфликт с Ф36 п.1, не лок

Ф36 п.1 переименовала пункт навигации «Подготовка к съёмке» → «С чего начать»,
но его `href` (`/#podgotovka`) указывал на секцию `Faq()`. Ф37 п.4 следом убрала
именно эту секцию целиком. Не поправить `href` значило бы оставить ссылку,
ведущую в никуда — тише 404, потому что просто ничего не происходит при клике.
**Решение билдера:** якорь `#podgotovka` переехал на `How()` (у неё не было
своего `id`) — семантически это она и есть: новое имя пункта навигации («С
чего начать») дословно совпадает с началом нового заголовка этой секции
(«С чего начать и как это всё устроено», Ф36 п.9). `href` не менялся, поменялся
только адресат. `Faq()` при выключении лишилась своего `id="podgotovka"` —
иначе при случайном одновременном возврате обоих компонентов на странице
оказались бы два узла с одним `id` (мина, невидимая на кадре, ловится только
замером — ровно то предупреждение методологии). Доказано selfcheck 6.3: якорь
ведёт на существующий узел, `#podgotovka` встречается ровно один раз.

### Другие находки и решения билдера

- **Ф36 п.6, класс заголовков.** FACTS.md называет `.t-h2`, в коде это `.t-h1`
  (см. пункт 6 выше). Проверено чтением `App.tsx`, не принято на слово лока/
  памяти — правило «лок/факт проверяется по источнику».
- **Композиция диптиха держит длину.** Новый текст обеих половин длиннее
  прежнего бюджета IA.md (BC-03, был конфликт на 21 слово при 14) — теперь
  ещё длиннее, но это дословный текст владелицы, не черновик для подгонки под
  бюджет; правка текста под макет запрещена. Композиция (`.t-quote`, диптих со
  сбитой осью) держит визуально — см. `shots/mood-01-quiet.png`,
  `shots/services-01-1280.png` (сосед по прокрутке) и полностраничные кадры.
- **Крючок съёмки `?priceplate=closed`.** Находка при первой попытке снять
  «закрытое» состояние плашки: `Locator.screenshot()` в Playwright сам
  прокручивает элемент во вьюпорт перед кадром, а прокрутка — ровно то
  действие, которое открывает дверцу. Без крючка «кадр закрытого состояния»
  на самом деле снимал уже открытое (тот же класс находки, что и `?rack=` у
  `useRack.ts`: у headless-браузера нет «просто посмотреть, не долистывая»).
  Добавлен `data-force-closed` в `PricePlate.tsx` + правило в `styles.css`,
  задействован только в `scripts/shots.mjs`; в продуктовом пути параметра нет.
- **`alt` шести новых кадров рэка и трёх заменённых кадров услуг** — написаны
  билдером по факту пикселей (каждый файл открыт как изображение перед
  написанием), предметным словарём, без «мягкого/нежного/воздушного света»
  (COPY § 0.14). Отбор кадров — координатора, не билдера; при следующей замене
  файлов `alt` переписываются вместе с ними (та же оговорка, что у рэка
  редакции 1, README § 4 п. 4).

### Самопроверка

`node scripts/selfcheck.mjs` переписан под новый текст и новое поведение:
- **VERBATIM** — сняты строки редакции, которых больше нет («Придумано
  заранее», «Найдено на месте», старый состав навигации), добавлены дословные
  фрагменты нового текста (нав, лид, диптих, услуги, шаги 1–4);
- **FORBIDDEN** — добавлены «Работы», обе метки диптиха, «Один человек»,
  «Весь прайс и условия», «Помогу подготовиться к съёмке» — каждая с причиной
  снятия;
- **5.2 переписан целиком** — была «цена скрыта и появляется по клику»
  (Ф29 п.6), стала «плашка не `<button>`, без `aria-expanded`, дверца закрыта
  до кадра и открыта после, полный текст цены в DOM всегда, формат «город —
  цена/час» на странице» (ОТМЕНА Ф29 п.6, симметрично инвертированная логика);
- **Новый раздел 6** — четыре измеримые проверки редакции 5: рэк 12 кадров и
  `altar` в числе первых двух, состав/порядок навигации дословно, якорь «С
  чего начать» ведёт на существующий узел без дублей `id`, пояснение папок
  раньше `.delivery-stage` в DOM.

**132 проверки, все зелёные**, код 0. **Проба на красноту** (`--redproof`) —
**28 проваленных** из инвертированной логики, код **1**: включая ВСЕ четыре
новых проверки раздела 6 (рэк «получено 12» при инвертированном условии,
навигация, якорь, порядок папок) — доказано, что новые проверки тоже умеют
краснеть, не только старые. `tsc --noEmit` — 0 ошибок; подложенная ошибка типа
(`const x: number = "нет"`) даёт `TS2322`, компилятор действительно смотрит
файлы.

### Доказательства

Съёмка — `node scripts/shots.mjs` при живом сервере на 5176, полный комплект
пересснят (46 кадров). Новый крючок `?priceplate=closed` — только в этом
скрипте. Кадры черновые (dev-сборка для критика), финальный evidence-бандл
снимает `dp-auditor` с прод-сборки.

| Что доказывает | Файлы |
|---|---|
| Первый экран с новым лидом и меньшей кнопкой, 4 ширины | `hero-{390,768,1280,1920}.png` |
| Навигация без «Работы», «Обо мне» вторым, «С чего начать» | `nav-01…05.png` |
| Рэк: 12 кадров, `altar` вторым после переворота | `rack-01-rest.png`, `rack-03-flipped.png` |
| Услуги: плотная верстка, новые цены, открытая плашка, 4-я карточка | `services-01-1280.png`, `services-02-390.png`, `services-03-1920.png` |
| Плашка цены: закрыта / открыта («дверца») | `price-plate-01-closed.png`, `price-plate-02-open.png` |
| «С чего начать и как это всё устроено»: новые шаги, папка после FAQ | `how-01-1280.png`, `how-02-390.png` |
| Диптих «Как получается кино»: цвет фона тихий/странный, метки сняты | `mood-01-quiet.png`, `mood-02-strange.png` |
| Вся страница целиком, 3 ширины | `page-full-{390,1280,1920}.png` |
| `prefers-reduced-motion`, рэк в сетку 12 кадров | `reduced-motion-full-{390,1280}.png` |

### Текущий статус

- `node scripts/selfcheck.mjs` — **зелёный**, 132/132.
- `./node_modules/.bin/tsc --noEmit` — **0 ошибок**.
- Живой сервер 5176 отдаёт страницу без ошибок консоли на 360/375/390/768/1280/1920.
- Открытые пункты, не закрытые этой редакцией: подписей у рэка по‑прежнему
  нет (теперь 12 кадров без подписей вместо 8 — тот же нерешённый вопрос,
  README § 4 п. 3), `alt`/подписи сетки «Шесть кадров» рассинхронны (секция
  выключена), блок отзывов пуст (OQ-05), юрстатус не возвращается (Ф17).
  `copy.offer.note`, `copy.offer.cta`, `copy.faq` и компонент `Faq()`
  сохранены дормантными — решение вернуть любую из этих зон на страницу
  принимает владелица, не билдер.

---

## 11. Плашка цены на тач по скроллу + веер на тач — перетаскивание

Дата: 2026-08-12. Две независимые правки, обе — прямой ответ владелицы в чате
(**не в `FACTS.md`** — с Ф39 журнал ведётся в сообщениях коммитов, здесь
сказано об этом коротко, полная цитата и постановка — в самом коммите).
Обе прошли через `apple-design` (лок), десктопный путь (`hover:hover`/
`pointer:fine`) ни в одной из них не тронут ни строкой.

### 11.1 Плашка цены на тач — двусторонний reveal по скроллу

Было: на вводе без курсора (`@media (hover: none)`) плашка стояла видна
ВСЕГДА и статично — другого триггера открытия, кроме курсора, не было.
Стало: та же CSS-анимация, что у `.offer-card:hover` (тайминги/задержки/
`--ease-soft` не переизобретены, скопированы), но триггер — атрибут
`data-in-view` на `.offer-card`, которым управляет новый хук
`usePriceReveal()` (`src/reveal.ts`).

**Находка, а не мнение** (поймана замером `getComputedStyle` во время
живого перехода, не увидена на кадре): первая редакция хука вешала
`IntersectionObserver` с `threshold: 1` прямо на `.offer-card` — и попадала
в петлю обратной связи. Плашка раскрывает место под собой
(`.offer-price`, `grid-template-rows: 0fr → 1fr`), карточка от этого
становится ВЫШЕ; если новый нижний край съезжает за экран, обсервер решает,
что карточка «уже не видна целиком», гасит плашку — карточка укорачивается,
снова видна целиком, обсервер открывает её опять, и так по кругу.
`grid-template-rows` дёргался между долями пикселя (0.05px, 7px, 6px)
вместо того чтобы дойти до раскрытого значения. Исправление — тот же приём,
что уже есть в этом файле у `useFocusScrub()` (Ф43): позиция и высота
каждой карточки меряются `getBoundingClientRect()` ОДИН РАЗ до первого
переключения (в этот момент ни одна плашка ещё не открыта, значит замер
застаёт именно закрытую высоту) и кэшируются; «видна целиком» дальше
решается по этой стабильной высоте, а не по живой — плашка физически не
может вырасти и вытолкнуть саму себя из критерия собственной видимости.
Подробный разбор — комментарий над `usePriceReveal()`.

При `prefers-reduced-motion: reduce` хук не подключается вовсе (правило
«нет контента без движения» — движения не будет, а не цены), CSS отдельно
возвращает плашку к прежнему «видна всегда, статично» (`styles.css`, блок
«ТАЧ ПРИ ВЫКЛЮЧЕННОМ ДВИЖЕНИИ»).

### 11.2 Веер на тач — перетаскивание пальцем, как у рэка

Владелица, отвечая на предложенный вариант (сетку миниатюр): «Стопку можно
листать пальцем, как фото на главной» — рэковская механика, не сетка и не
разворот по тапу. `computeFanMode()` в `src/Fan.tsx` разведён с двух причин
на три:

| Условие | Режим | Поведение |
|---|---|---|
| `hover:hover` + `pointer:fine`, движение разрешено | `stack` | без изменений — наведение раскрывает ряд, наведение на кадр увеличивает его |
| грубый указатель (тач), движение разрешено | `drag` (новый) | `useRack(photos.length)` — та же физика, что у `Rack.tsx`, разметка `FanDrag` в `Fan.tsx` |
| `prefers-reduced-motion: reduce`, любой ввод | `flat` | без изменений — статичная сетка, все кадры видны разом |

Раньше `'flat'` означал «нет точного курсора ИЛИ движение выключено» —
одна причина на двоих. Теперь тач сам по себе даёт `'drag'`, а `'flat'`
достижим только через выключенное движение: драг-жест требует движения, и
при `prefers-reduced-motion` его быть не должно, поэтому сетка остаётся
сеткой независимо от типа ввода.

Общий компонент — эффект в обоих местах, где живёт `Fan`: карточки услуг
на главной (`variant="compact"`) и экран результата раскадровки
(`PhotoFan.tsx`). Размер драг-кадра подобран замером (не на глаз):
реальное место в карточке услуги на 375/390 px — 339/354 px
(`getBoundingClientRect` контейнера), CSS берёт `min(74%, кап)` от него,
кап разный для `compact` (18rem) и `full` (26rem) — подробности в CSS-блоке
«ВЕЕР НА ТАЧ-ЭКРАНЕ — ПЕРЕТАСКИВАНИЕ ПАЛЬЦЕМ».

`scripts/selfcheck-storyboard.mjs`, раздел «5b», проверял именно старую
семантику («тач → flat») — с разведением причин это перестало быть
применимо к простому тач-вводу без reduced-motion, не удалено молча:
переписано на 5b.1 (тач, движение разрешено → `drag`, новая механика:
клавиатура, кнопка, все фото в DOM) и 5b.2 (тач + reduced-motion
одновременно → всё ещё `flat`, регрессионная проверка именно на это
сочетание).

### Самопроверка

`node scripts/selfcheck.mjs` и `node scripts/selfcheck-storyboard.mjs` —
зелёные целиком (включая ветки, не тронутые этой правкой). `--redproof` на
обоих подтверждён отдельно для новых/изменённых проверок — они умеют
краснеть. `tsc --noEmit` — 0 ошибок. Проверено Playwright-эмуляцией тача
(`hover:none`/`pointer:coarse` через подмену `matchMedia`, как уже делает
`selfcheck-storyboard.mjs`) и `hasTouch: true` на 360/375/390/768/1280/1920
— горизонтальной прокрутки нет ни на одной ширине ни в одном режиме,
консоль чиста. Отдельно измерено: держание скролла на границе
«карточка целиком видна» в течение ~1,2 с не даёт колебаний `data-in-view`
ни на одной из четырёх карточек услуг (0 переключений за окно наблюдения)
— доказательство того, что находка про петлю обратной связи (11.1)
действительно исправлена, а не просто перестала быть заметна на глаз.
