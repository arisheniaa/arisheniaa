# Сайт открыт для индексации целиком: закрывать здесь нечего — обе страницы
# и все фотографии и есть его содержимое.
#
# Файл лежит в `public/` и попадает в корень сборки как есть. Класть его в
# `dist/` руками бессмысленно: `dist/` не хранится в гите и пересоздаётся
# при каждой сборке.
User-agent: *
Allow: /

# Карта сайта. Адрес абсолютный — по стандарту относительный здесь не
# допускается, и поисковик просто пропустит строку, не сообщив об ошибке.
Sitemap: https://arisheniaa.ru/sitemap.xml
